type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - DENIED
    - VIOLATED
  notEq:
    type: string
    enum:
    - DENIED
    - VIOLATED
  in:
    type: array
    items:
      type: string
      enum:
      - DENIED
      - VIOLATED
  notIn:
    type: array
    items:
      type: string
      enum:
      - DENIED
      - VIOLATED
  isNull:
    type: boolean
