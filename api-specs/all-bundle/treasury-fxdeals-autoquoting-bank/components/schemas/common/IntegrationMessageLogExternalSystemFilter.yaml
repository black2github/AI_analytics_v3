type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - ASK
    - PKP
  notEq:
    type: string
    enum:
    - ASK
    - PKP
  in:
    type: array
    items:
      type: string
      enum:
      - ASK
      - PKP
  notIn:
    type: array
    items:
      type: string
      enum:
      - ASK
      - PKP
  isNull:
    type: boolean
