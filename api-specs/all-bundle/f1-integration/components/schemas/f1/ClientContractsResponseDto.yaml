type: object
properties:
  rc:
    type: integer
    description: Результат выполнения
    format: int64
    example: 0
  error:
    type: string
    description: Текст ошибки если есть
    example: Some error text
  clientGUID:
    type: string
    format: uuid
  clientRegNum:
    type: string
  contracts:
    type: array
    items:
      $ref: ./ContractDto.yaml
  reservedAccounts:
    type: array
    items:
      $ref: ./ReservedAccountDto.yaml
description: Ответа на запрос контрактов клиента
