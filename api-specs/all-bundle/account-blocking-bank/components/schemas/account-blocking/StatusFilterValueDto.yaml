type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    description: Статус блокировки документа
    enum:
    - IMPORT_ERROR
    - ACTIVE
    - INACTIVE
  notEq:
    type: string
    description: Статус блокировки документа
    enum:
    - IMPORT_ERROR
    - ACTIVE
    - INACTIVE
  in:
    type: array
    items:
      type: string
      description: Статус блокировки документа
      enum:
      - IMPORT_ERROR
      - ACTIVE
      - INACTIVE
  notIn:
    type: array
    items:
      type: string
      description: Статус блокировки документа
      enum:
      - IMPORT_ERROR
      - ACTIVE
      - INACTIVE
  isNull:
    type: boolean
description: 1.4 Статус. Содержится в списке
