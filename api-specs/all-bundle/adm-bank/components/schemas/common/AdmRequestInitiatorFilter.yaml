type: object
properties:
  contains:
    type: string
  eq:
    type: string
    enum:
    - BOL
    - INFOR
    - BANNER
  in:
    type: array
    items:
      type: string
      enum:
      - BOL
      - INFOR
      - BANNER
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    enum:
    - BOL
    - INFOR
    - BANNER
  notIn:
    type: array
    items:
      type: string
      enum:
      - BOL
      - INFOR
      - BANNER
description: Фильтр по инициатору подачи заявки
