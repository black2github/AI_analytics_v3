type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - ONLINE
    - PAPER
    - ONLINE_EPGU_CONFIRMATION
  notEq:
    type: string
    enum:
    - ONLINE
    - PAPER
    - ONLINE_EPGU_CONFIRMATION
  in:
    type: array
    items:
      type: string
      enum:
      - ONLINE
      - PAPER
      - ONLINE_EPGU_CONFIRMATION
  notIn:
    type: array
    items:
      type: string
      enum:
      - ONLINE
      - PAPER
      - ONLINE_EPGU_CONFIRMATION
  isNull:
    type: boolean
description: Форма отправки (eq,in)
