type: object
properties:
  contains:
    type: string
  eq:
    type: string
    description: Статус АДМ
    enum:
    - NEW
    - DELIVERING
    - PREPARING
    - READY_TO_WORK
    - WORK
    - BLOCKED
    - WAIT_DEMOUNTING
    - REMOVED
    - AWAITING_SHIPMENT
    - IN_TRANSIT
  in:
    type: array
    items:
      type: string
      description: Статус АДМ
      enum:
      - NEW
      - DELIVERING
      - PREPARING
      - READY_TO_WORK
      - WORK
      - BLOCKED
      - WAIT_DEMOUNTING
      - REMOVED
      - AWAITING_SHIPMENT
      - IN_TRANSIT
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    description: Статус АДМ
    enum:
    - NEW
    - DELIVERING
    - PREPARING
    - READY_TO_WORK
    - WORK
    - BLOCKED
    - WAIT_DEMOUNTING
    - REMOVED
    - AWAITING_SHIPMENT
    - IN_TRANSIT
  notIn:
    type: array
    items:
      type: string
      description: Статус АДМ
      enum:
      - NEW
      - DELIVERING
      - PREPARING
      - READY_TO_WORK
      - WORK
      - BLOCKED
      - WAIT_DEMOUNTING
      - REMOVED
      - AWAITING_SHIPMENT
      - IN_TRANSIT
description: Фильтр по статусу АДМ
