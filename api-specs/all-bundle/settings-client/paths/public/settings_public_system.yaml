get:
  tags:
  - АПИ публичного пользователя для работы с системными настройками
  summary: Получение всех доступных настроек
  operationId: getAll_2
  responses:
    '200':
      description: OK
      content:
        '*/*':
          schema:
            uniqueItems: true
            type: array
            items:
              $ref: ../../components/schemas/common/SettingDto.yaml
