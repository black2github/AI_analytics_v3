get:
  tags:
  - АПИ Клиента для работы с системными настройками
  summary: Получение всех доступных настроек
  description: 'Привилегии: SETTINGS.CLIENT.SYSTEM_SETTINGS_VIEW'
  operationId: getAll_1
  responses:
    '200':
      description: OK
      content:
        '*/*':
          schema:
            uniqueItems: true
            type: array
            items:
              $ref: ../../components/schemas/common/SettingDto.yaml
