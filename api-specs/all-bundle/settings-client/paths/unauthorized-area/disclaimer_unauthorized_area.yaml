get:
  tags:
  - АПИ клиента для работы с дисклеймерами
  summary: Получить список записей в непривилегированной зоне
  operationId: getInUnauthorizedArea
  parameters:
  - name: Locale-code
    in: header
    required: true
    schema:
      type: string
      enum:
      - UNKNOWN
      - RU
      - EN
  responses:
    '200':
      description: OK
      content:
        '*/*':
          schema:
            type: array
            items:
              $ref: ../../components/schemas/common/DisclaimerClientDto.yaml
