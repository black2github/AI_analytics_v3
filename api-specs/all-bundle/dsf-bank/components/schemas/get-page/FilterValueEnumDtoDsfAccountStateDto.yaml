type: object
properties:
  contains:
    type: string
  eq:
    type: string
    enum:
    - RESERVED
    - ANNULLED
    - OPENED
    - CLOSED
    - TO_OPEN
    - TO_CLOSE
  in:
    type: array
    items:
      type: string
      enum:
      - RESERVED
      - ANNULLED
      - OPENED
      - CLOSED
      - TO_OPEN
      - TO_CLOSE
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    enum:
    - RESERVED
    - ANNULLED
    - OPENED
    - CLOSED
    - TO_OPEN
    - TO_CLOSE
  notIn:
    type: array
    items:
      type: string
      enum:
      - RESERVED
      - ANNULLED
      - OPENED
      - CLOSED
      - TO_OPEN
      - TO_CLOSE
