type: object
properties:
  contains:
    type: string
  eq:
    type: string
    description: Результат первичных проверок
    enum:
    - POSITIVE
    - NEGATIVE
    - ERROR
    - CONDITIONALLY_POSITIVE
    - CONDITIONALLY_POSITIVE_BY_FNS
    - CONDITIONALLY_POSITIVE_BY_FTS
    - EMPTY
  in:
    type: array
    items:
      type: string
      description: Результат первичных проверок
      enum:
      - POSITIVE
      - NEGATIVE
      - ERROR
      - CONDITIONALLY_POSITIVE
      - CONDITIONALLY_POSITIVE_BY_FNS
      - CONDITIONALLY_POSITIVE_BY_FTS
      - EMPTY
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    description: Результат первичных проверок
    enum:
    - POSITIVE
    - NEGATIVE
    - ERROR
    - CONDITIONALLY_POSITIVE
    - CONDITIONALLY_POSITIVE_BY_FNS
    - CONDITIONALLY_POSITIVE_BY_FTS
    - EMPTY
  notIn:
    type: array
    items:
      type: string
      description: Результат первичных проверок
      enum:
      - POSITIVE
      - NEGATIVE
      - ERROR
      - CONDITIONALLY_POSITIVE
      - CONDITIONALLY_POSITIVE_BY_FNS
      - CONDITIONALLY_POSITIVE_BY_FTS
      - EMPTY
description: Результаты первичных проверок. Полное совпадение (eq)
