type: object
properties:
  contains:
    type: string
  eq:
    type: string
    description: Статус выдачи прав доступа и отправки инвайта
    enum:
    - ACCESS_GRANTED_INVITE_SENT
    - ERROR_ACCESS_GRANTED_INVITE_SENT
    - ACCESS_AND_INVITE_REVOKED
    - ERROR_ACCESS_AND_INVITE_REVOKED
    - ACCESS_AND_INVITE_NOT_GRANTED
    - INVITE_SENT
    - ERROR_INVITE_SENT
  in:
    type: array
    items:
      type: string
      description: Статус выдачи прав доступа и отправки инвайта
      enum:
      - ACCESS_GRANTED_INVITE_SENT
      - ERROR_ACCESS_GRANTED_INVITE_SENT
      - ACCESS_AND_INVITE_REVOKED
      - ERROR_ACCESS_AND_INVITE_REVOKED
      - ACCESS_AND_INVITE_NOT_GRANTED
      - INVITE_SENT
      - ERROR_INVITE_SENT
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    description: Статус выдачи прав доступа и отправки инвайта
    enum:
    - ACCESS_GRANTED_INVITE_SENT
    - ERROR_ACCESS_GRANTED_INVITE_SENT
    - ACCESS_AND_INVITE_REVOKED
    - ERROR_ACCESS_AND_INVITE_REVOKED
    - ACCESS_AND_INVITE_NOT_GRANTED
    - INVITE_SENT
    - ERROR_INVITE_SENT
  notIn:
    type: array
    items:
      type: string
      description: Статус выдачи прав доступа и отправки инвайта
      enum:
      - ACCESS_GRANTED_INVITE_SENT
      - ERROR_ACCESS_GRANTED_INVITE_SENT
      - ACCESS_AND_INVITE_REVOKED
      - ERROR_ACCESS_AND_INVITE_REVOKED
      - ACCESS_AND_INVITE_NOT_GRANTED
      - INVITE_SENT
      - ERROR_INVITE_SENT
description: Статус выдачи прав доступа и отправки инвайта. Полное совпадение (eq)
