type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - NEW
    - DELETED
    - SIGNED_1
    - SIGNED_2
    - SIGNED
    - DELIVERED
    - SIGN_VALID
    - DETAILS_VALID
    - ACCEPTED
    - EXPORT_TO_ABS
    - EXPORTED_TO_ABS
    - EXPORT_TO_ABS_ERROR
    - ACCEPT_FOR_EXECUTION
    - COMPLETED
    - REJECTED
    - WAITING_FOR_CANCEL_BASE_DOCUMENT
  notEq:
    type: string
    enum:
    - NEW
    - DELETED
    - SIGNED_1
    - SIGNED_2
    - SIGNED
    - DELIVERED
    - SIGN_VALID
    - DETAILS_VALID
    - ACCEPTED
    - EXPORT_TO_ABS
    - EXPORTED_TO_ABS
    - EXPORT_TO_ABS_ERROR
    - ACCEPT_FOR_EXECUTION
    - COMPLETED
    - REJECTED
    - WAITING_FOR_CANCEL_BASE_DOCUMENT
  in:
    type: array
    items:
      type: string
      enum:
      - NEW
      - DELETED
      - SIGNED_1
      - SIGNED_2
      - SIGNED
      - DELIVERED
      - SIGN_VALID
      - DETAILS_VALID
      - ACCEPTED
      - EXPORT_TO_ABS
      - EXPORTED_TO_ABS
      - EXPORT_TO_ABS_ERROR
      - ACCEPT_FOR_EXECUTION
      - COMPLETED
      - REJECTED
      - WAITING_FOR_CANCEL_BASE_DOCUMENT
  notIn:
    type: array
    items:
      type: string
      enum:
      - NEW
      - DELETED
      - SIGNED_1
      - SIGNED_2
      - SIGNED
      - DELIVERED
      - SIGN_VALID
      - DETAILS_VALID
      - ACCEPTED
      - EXPORT_TO_ABS
      - EXPORTED_TO_ABS
      - EXPORT_TO_ABS_ERROR
      - ACCEPT_FOR_EXECUTION
      - COMPLETED
      - REJECTED
      - WAITING_FOR_CANCEL_BASE_DOCUMENT
  isNull:
    type: boolean
description: Статус документа. Поиск по  Запрос на отзыв.<Статус документа>. Равенство, содержится в списке
