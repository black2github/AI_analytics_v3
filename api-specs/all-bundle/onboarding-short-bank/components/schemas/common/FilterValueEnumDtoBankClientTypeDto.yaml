type: object
properties:
  contains:
    type: string
  eq:
    type: string
    description: Тип клиента
    enum:
    - UL
    - IP
  in:
    type: array
    items:
      type: string
      description: Тип клиента
      enum:
      - UL
      - IP
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    description: Тип клиента
    enum:
    - UL
    - IP
  notIn:
    type: array
    items:
      type: string
      description: Тип клиента
      enum:
      - UL
      - IP
description: Тип Клиента. Полное совпадение (eq)
