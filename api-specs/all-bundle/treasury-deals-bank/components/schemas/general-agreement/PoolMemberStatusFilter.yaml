type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - NONE
    - AGREEMENT_ACTIVE
    - AGREEMENT_ENDED
    - AGREEMENT_BLOCKED
    - AGREEMENT_DELETED
    - REQUEST_IN_PROGRESS
    - REQUEST_REJECTED
    - REQUEST_REJECTED_BY_CLIENT
    - REQUEST_ACCEPTED
    - REQUEST_EXECUTED
  notEq:
    type: string
    enum:
    - NONE
    - AGREEMENT_ACTIVE
    - AGREEMENT_ENDED
    - AGREEMENT_BLOCKED
    - AGREEMENT_DELETED
    - REQUEST_IN_PROGRESS
    - REQUEST_REJECTED
    - REQUEST_REJECTED_BY_CLIENT
    - REQUEST_ACCEPTED
    - REQUEST_EXECUTED
  in:
    type: array
    items:
      type: string
      enum:
      - NONE
      - AGREEMENT_ACTIVE
      - AGREEMENT_ENDED
      - AGREEMENT_BLOCKED
      - AGREEMENT_DELETED
      - REQUEST_IN_PROGRESS
      - REQUEST_REJECTED
      - REQUEST_REJECTED_BY_CLIENT
      - REQUEST_ACCEPTED
      - REQUEST_EXECUTED
  notIn:
    type: array
    items:
      type: string
      enum:
      - NONE
      - AGREEMENT_ACTIVE
      - AGREEMENT_ENDED
      - AGREEMENT_BLOCKED
      - AGREEMENT_DELETED
      - REQUEST_IN_PROGRESS
      - REQUEST_REJECTED
      - REQUEST_REJECTED_BY_CLIENT
      - REQUEST_ACCEPTED
      - REQUEST_EXECUTED
  isNull:
    type: boolean
