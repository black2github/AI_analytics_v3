type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - ASK
    - PKP
  notEq:
    type: string
    enum:
    - ASK
    - PKP
  in:
    type: array
    items:
      type: string
      enum:
      - ASK
      - PKP
  notIn:
    type: array
    items:
      type: string
      enum:
      - ASK
      - PKP
  isNull:
    type: boolean
  gt:
    type: string
    enum:
    - ASK
    - PKP
  ge:
    type: string
    enum:
    - ASK
    - PKP
  lt:
    type: string
    enum:
    - ASK
    - PKP
  le:
    type: string
    enum:
    - ASK
    - PKP
