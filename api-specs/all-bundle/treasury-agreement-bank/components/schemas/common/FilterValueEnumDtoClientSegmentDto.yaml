type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - ALL
    - MSB
    - KIK
    - NOT_FIELD
  notEq:
    type: string
    enum:
    - ALL
    - MSB
    - KIK
    - NOT_FIELD
  in:
    type: array
    items:
      type: string
      enum:
      - ALL
      - MSB
      - KIK
      - NOT_FIELD
  notIn:
    type: array
    items:
      type: string
      enum:
      - ALL
      - MSB
      - KIK
      - NOT_FIELD
  isNull:
    type: boolean
description: Признак маршрутизации
