type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - ALL
    - RESIDENT
    - NOT_RESIDENT
  notEq:
    type: string
    enum:
    - ALL
    - RESIDENT
    - NOT_RESIDENT
  in:
    type: array
    items:
      type: string
      enum:
      - ALL
      - RESIDENT
      - NOT_RESIDENT
  notIn:
    type: array
    items:
      type: string
      enum:
      - ALL
      - RESIDENT
      - NOT_RESIDENT
  isNull:
    type: boolean
description: Признак резидента
