post:
  tags:
  - Заявка на заключение генерального соглашения. Операции подписи для нескольких документов.
  - Скроллер и общее API
  summary: Сформировать данные для функции подписи для нескольких документов
  description: 'Привилегии: (#eco.userTypeIs(''BANK'') OR #eco.userTypeIs(''TECHNICAL'')) AND hasAuthority(''TREASURY_AGREEMENT_REQUEST_DEPOSIT_SIGN'') AND hasAuthority(''TREASURY_AGREEMENT_REQUEST_MNO_SIGN'') AND hasAuthority(''TREASURY_AGREEMENT_REQUEST_CONVERSION_SIGN'') AND hasAuthority(''TREASURY_AGREEMENT_REQUEST_DU_DEPOSIT_SIGN'') AND hasAuthority(''TREASURY_AGREEMENT_REQUEST_DU_MNO_SIGN'') AND hasAuthority(''TREASURY_AGREEMENT_REQUEST_DU_CONVERSION_SIGN'') AND hasAuthority(''TREASURY_AGREEMENT_REQUEST_GSNO_PARENT_SIGN'') AND hasAuthority(''TREASURY_AGREEMENT_REQUEST_GSNO_CHILD_HEAD_SIGN'') AND hasAuthority(''TREASURY_AGREEMENT_REQUEST_GSNO_CHILD_FRIEND_SIGN'')'
  operationId: generateSignDataBatch
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../components/schemas/misc/GenericCommandListUUID.yaml
    required: true
  responses:
    '200':
      description: OK
      content:
        '*/*':
          schema:
            $ref: ../../components/schemas/misc/GenericResponseErrorDataListGenericResponseErrorDataUniSignDataTreasuryDto.yaml
    '500':
      description: Internal Server Error
      content:
        '*/*':
          schema:
            $ref: ../../components/schemas/common/GenericResponseErrorDataObject.yaml
    '403':
      description: Forbidden
      content:
        '*/*':
          schema:
            $ref: ../../components/schemas/common/ActionResult.yaml
    '400':
      description: Bad Request
      content:
        '*/*':
          schema:
            $ref: ../../components/schemas/common/ActionResultObject.yaml
    '423':
      description: Locked
      content:
        '*/*':
          schema:
            $ref: ../../components/schemas/common/ActionResultObject.yaml
    '415':
      description: Unsupported Media Type
      content:
        '*/*':
          schema:
            $ref: ../../components/schemas/common/ActionResultObject.yaml
    '401':
      description: Unauthorized
      content:
        '*/*':
          schema:
            $ref: ../../components/schemas/common/ActionResultObject.yaml
