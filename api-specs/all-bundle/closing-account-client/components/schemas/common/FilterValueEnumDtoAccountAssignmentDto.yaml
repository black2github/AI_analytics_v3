type: object
properties:
  contains:
    type: string
  eq:
    type: string
    enum:
    - BASE
    - GPS_GOZ
  in:
    type: array
    items:
      type: string
      enum:
      - BASE
      - GPS_GOZ
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    enum:
    - BASE
    - GPS_GOZ
  notIn:
    type: array
    items:
      type: string
      enum:
      - BASE
      - GPS_GOZ
description: Назначение счёта (EQ)
