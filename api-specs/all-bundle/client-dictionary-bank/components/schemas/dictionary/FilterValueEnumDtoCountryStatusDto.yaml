type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - ACTIVE
    - DELETED
  notEq:
    type: string
    enum:
    - ACTIVE
    - DELETED
  in:
    type: array
    items:
      type: string
      enum:
      - ACTIVE
      - DELETED
  notIn:
    type: array
    items:
      type: string
      enum:
      - ACTIVE
      - DELETED
  isNull:
    type: boolean
description: Поиск по статусу записи страны
