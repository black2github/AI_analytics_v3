type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - BLOCK
    - INFORM_ONLY
    - OPEN
    - RECEPTION_ONLY
  notEq:
    type: string
    enum:
    - BLOCK
    - INFORM_ONLY
    - OPEN
    - RECEPTION_ONLY
  in:
    type: array
    items:
      type: string
      enum:
      - BLOCK
      - INFORM_ONLY
      - OPEN
      - RECEPTION_ONLY
  notIn:
    type: array
    items:
      type: string
      enum:
      - BLOCK
      - INFORM_ONLY
      - OPEN
      - RECEPTION_ONLY
  isNull:
    type: boolean
description: Тип действия над счетом представителя Клиента ДБО БСС. Фильтрация по IN.
