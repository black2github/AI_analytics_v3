type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - BLOCK
    - INFORM_ONLY
    - OPEN
    - RECEPTION_ONLY
    - EXTENDED
  notEq:
    type: string
    enum:
    - BLOCK
    - INFORM_ONLY
    - OPEN
    - RECEPTION_ONLY
    - EXTENDED
  in:
    type: array
    items:
      type: string
      enum:
      - BLOCK
      - INFORM_ONLY
      - OPEN
      - RECEPTION_ONLY
      - EXTENDED
  notIn:
    type: array
    items:
      type: string
      enum:
      - BLOCK
      - INFORM_ONLY
      - OPEN
      - RECEPTION_ONLY
      - EXTENDED
  isNull:
    type: boolean
description: Режим доступа к счету
