type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - ALL
    - CONNECTED
    - NOT_CONNECTED
  notEq:
    type: string
    enum:
    - ALL
    - CONNECTED
    - NOT_CONNECTED
  in:
    type: array
    items:
      type: string
      enum:
      - ALL
      - CONNECTED
      - NOT_CONNECTED
  notIn:
    type: array
    items:
      type: string
      enum:
      - ALL
      - CONNECTED
      - NOT_CONNECTED
  isNull:
    type: boolean
description: Признак подключения к ДБО
