type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    description: Актуальная запись с учетом значения "Не определено"
    enum:
    - ACTUAL
    - NOT_ACTUAL
    - NOT_DEFINED
  notEq:
    type: string
    description: Актуальная запись с учетом значения "Не определено"
    enum:
    - ACTUAL
    - NOT_ACTUAL
    - NOT_DEFINED
  in:
    type: array
    items:
      type: string
      description: Актуальная запись с учетом значения "Не определено"
      enum:
      - ACTUAL
      - NOT_ACTUAL
      - NOT_DEFINED
  notIn:
    type: array
    items:
      type: string
      description: Актуальная запись с учетом значения "Не определено"
      enum:
      - ACTUAL
      - NOT_ACTUAL
      - NOT_DEFINED
  isNull:
    type: boolean
description: Актуальная запись с учетом значения "Не определено" (полное соответствие). V2.
