get:
  tags:
  - API Банка для работы с ДВК.
  summary: Получить токен файла по attachmentId
  description: 'Привилегии: DVK_BANK.DOWNLOAD.FILE'
  operationId: generateDownloadToken
  parameters:
  - name: id
    in: path
    required: true
    schema:
      type: string
      format: uuid
  responses:
    '200':
      description: OK
      content:
        '*/*':
          schema:
            $ref: ../../components/schemas/document/GenericResponseDocumentErrorDocumentFileResponse.yaml
