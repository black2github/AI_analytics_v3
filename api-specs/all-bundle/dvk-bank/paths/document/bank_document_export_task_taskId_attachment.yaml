get:
  tags:
  - Банковский API документов валютного контроля. Операции экспорта реестра через задания
  summary: Получить информацию для скачивания реестра документов валютного контроля
  description: 'Привилегии: DVK_BANK.EXPORT'
  operationId: getExportRegistryTaskAttachment
  parameters:
  - name: taskId
    in: path
    required: true
    schema:
      type: string
      format: uuid
  responses:
    '200':
      description: OK
      content:
        '*/*':
          schema:
            $ref: ../../components/schemas/document/GenericResponseDocumentErrorExportRegistryAttachmentDto.yaml
