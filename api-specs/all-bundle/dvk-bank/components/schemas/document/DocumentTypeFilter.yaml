type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - CONF_DOC_CERT
    - REG_CONTRACT
    - RENEW_CONTRACT
    - REG_CRED_AGREEMENT
    - RENEW_CRED_AGREEMENT
    - CONTR_CLOSE
  notEq:
    type: string
    enum:
    - CONF_DOC_CERT
    - REG_CONTRACT
    - RENEW_CONTRACT
    - REG_CRED_AGREEMENT
    - RENEW_CRED_AGREEMENT
    - CONTR_CLOSE
  in:
    type: array
    items:
      type: string
      enum:
      - CONF_DOC_CERT
      - REG_CONTRACT
      - RENEW_CONTRACT
      - REG_CRED_AGREEMENT
      - RENEW_CRED_AGREEMENT
      - CONTR_CLOSE
  notIn:
    type: array
    items:
      type: string
      enum:
      - CONF_DOC_CERT
      - REG_CONTRACT
      - RENEW_CONTRACT
      - REG_CRED_AGREEMENT
      - RENEW_CRED_AGREEMENT
      - CONTR_CLOSE
  isNull:
    type: boolean
description: Тип документа (eq|in)
