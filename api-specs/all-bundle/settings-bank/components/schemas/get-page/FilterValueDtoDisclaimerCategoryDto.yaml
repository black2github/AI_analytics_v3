type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    description: Категории дисклеймера
    enum:
    - ENABLED
    - DISABLED
  notEq:
    type: string
    description: Категории дисклеймера
    enum:
    - ENABLED
    - DISABLED
  in:
    type: array
    items:
      type: string
      description: Категории дисклеймера
      enum:
      - ENABLED
      - DISABLED
  notIn:
    type: array
    items:
      type: string
      description: Категории дисклеймера
      enum:
      - ENABLED
      - DISABLED
  isNull:
    type: boolean
  gt:
    type: string
    description: Категории дисклеймера
    enum:
    - ENABLED
    - DISABLED
  ge:
    type: string
    description: Категории дисклеймера
    enum:
    - ENABLED
    - DISABLED
  lt:
    type: string
    description: Категории дисклеймера
    enum:
    - ENABLED
    - DISABLED
  le:
    type: string
    description: Категории дисклеймера
    enum:
    - ENABLED
    - DISABLED
description: Категория. Признак необходимости отображать дисклеймер пользователю Клиента
