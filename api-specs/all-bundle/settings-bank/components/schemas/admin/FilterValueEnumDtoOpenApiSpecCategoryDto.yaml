type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - INPUT
    - OUTPUT
  notEq:
    type: string
    enum:
    - INPUT
    - OUTPUT
  in:
    type: array
    items:
      type: string
      enum:
      - INPUT
      - OUTPUT
  notIn:
    type: array
    items:
      type: string
      enum:
      - INPUT
      - OUTPUT
  isNull:
    type: boolean
description: Категория спецификации (eq)
