type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - EXAMPLE
  notEq:
    type: string
    enum:
    - EXAMPLE
  in:
    type: array
    items:
      type: string
      enum:
      - EXAMPLE
  notIn:
    type: array
    items:
      type: string
      enum:
      - EXAMPLE
  isNull:
    type: boolean
description: Тип фичи
