type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - PUBLIC
    - BANK_PUBLIC
    - CLIENT
    - BANK
    - USER
  notEq:
    type: string
    enum:
    - PUBLIC
    - BANK_PUBLIC
    - CLIENT
    - BANK
    - USER
  in:
    type: array
    items:
      type: string
      enum:
      - PUBLIC
      - BANK_PUBLIC
      - CLIENT
      - BANK
      - USER
  notIn:
    type: array
    items:
      type: string
      enum:
      - PUBLIC
      - BANK_PUBLIC
      - CLIENT
      - BANK
      - USER
  isNull:
    type: boolean
description: Тип настройки
