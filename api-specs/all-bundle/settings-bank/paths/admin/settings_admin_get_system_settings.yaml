post:
  tags:
  - АПИ Банка для управления настройками
  summary: Получить системные настройки по коду
  description: 'Привилегии: SETTINGS_VIEW'
  operationId: getSystemSettings
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../../components/schemas/admin/GetSystemSettingsRequest.yaml
    required: true
  responses:
    '200':
      description: OK
      content:
        '*/*':
          schema:
            uniqueItems: true
            type: array
            items:
              $ref: ../../components/schemas/admin/SettingAdminDto.yaml
