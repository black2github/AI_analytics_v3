type: object
properties:
  contains:
    type: string
  eq:
    type: string
    description: Класс типа письма
    enum:
    - MAIL_TO_BANK
    - MAIL_FROM_BANK
    - REPLY_MAIL_TO_BANK
  in:
    type: array
    items:
      type: string
      description: Класс типа письма
      enum:
      - MAIL_TO_BANK
      - MAIL_FROM_BANK
      - REPLY_MAIL_TO_BANK
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    description: Класс типа письма
    enum:
    - MAIL_TO_BANK
    - MAIL_FROM_BANK
    - REPLY_MAIL_TO_BANK
  notIn:
    type: array
    items:
      type: string
      description: Класс типа письма
      enum:
      - MAIL_TO_BANK
      - MAIL_FROM_BANK
      - REPLY_MAIL_TO_BANK
description: Класс документа. Группа фильтров.
