type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - CREDIT
    - DEBIT
    - ALL
  notEq:
    type: string
    enum:
    - CREDIT
    - DEBIT
    - ALL
  in:
    type: array
    items:
      type: string
      enum:
      - CREDIT
      - DEBIT
      - ALL
  notIn:
    type: array
    items:
      type: string
      enum:
      - CREDIT
      - DEBIT
      - ALL
  isNull:
    type: boolean
description: Фильтр по "Поступление/списание" (равенство)
