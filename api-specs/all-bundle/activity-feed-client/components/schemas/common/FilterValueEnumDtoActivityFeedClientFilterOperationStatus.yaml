type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - PROCESSING
    - REJECTED
    - EXECUTED
  notEq:
    type: string
    enum:
    - PROCESSING
    - REJECTED
    - EXECUTED
  in:
    type: array
    items:
      type: string
      enum:
      - PROCESSING
      - REJECTED
      - EXECUTED
  notIn:
    type: array
    items:
      type: string
      enum:
      - PROCESSING
      - REJECTED
      - EXECUTED
  isNull:
    type: boolean
description: Фильтр по "Статус документа" (равенство, содержится в списке)
