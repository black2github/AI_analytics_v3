type: object
properties:
  contains:
    type: string
  eq:
    type: string
    description: Источник создания заявки
    enum:
    - LANDING_PAGE
    - CRM_INFOR
    - ONBOARDING
  in:
    type: array
    items:
      type: string
      description: Источник создания заявки
      enum:
      - LANDING_PAGE
      - CRM_INFOR
      - ONBOARDING
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    description: Источник создания заявки
    enum:
    - LANDING_PAGE
    - CRM_INFOR
    - ONBOARDING
  notIn:
    type: array
    items:
      type: string
      description: Источник создания заявки
      enum:
      - LANDING_PAGE
      - CRM_INFOR
      - ONBOARDING
description: Источник создания заявки. Множественный выбор (in)
