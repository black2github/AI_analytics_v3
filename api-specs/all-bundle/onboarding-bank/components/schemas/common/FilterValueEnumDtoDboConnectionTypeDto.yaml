type: object
properties:
  contains:
    type: string
  eq:
    type: string
    description: Тип подключения ДБО
    enum:
    - KB_WEB
    - GPB_BOL
  in:
    type: array
    items:
      type: string
      description: Тип подключения ДБО
      enum:
      - KB_WEB
      - GPB_BOL
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    description: Тип подключения ДБО
    enum:
    - KB_WEB
    - GPB_BOL
  notIn:
    type: array
    items:
      type: string
      description: Тип подключения ДБО
      enum:
      - KB_WEB
      - GPB_BOL
description: Тип подключения ДБО
