type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - CAT_DRAFT
    - CAT_QUOTED
    - CAT_NEW
    - CAT_READY_TO_SIGN
    - CAT_WAITING_FOR_VISA
    - CAT_VISA_REJECTED
    - CAT_DELIVERED
    - CAT_CANCELED
    - CAT_DENIED
    - CAT_CONFIRMED
    - CAT_DONE
    - CAT_WITHDRAWN
    - CAT_VIOLATED
    - CAT_EXECUTED
    - CAT_WITHDRAWN_EXECUTED
  notEq:
    type: string
    enum:
    - CAT_DRAFT
    - CAT_QUOTED
    - CAT_NEW
    - CAT_READY_TO_SIGN
    - CAT_WAITING_FOR_VISA
    - CAT_VISA_REJECTED
    - CAT_DELIVERED
    - CAT_CANCELED
    - CAT_DENIED
    - CAT_CONFIRMED
    - CAT_DONE
    - CAT_WITHDRAWN
    - CAT_VIOLATED
    - CAT_EXECUTED
    - CAT_WITHDRAWN_EXECUTED
  in:
    type: array
    items:
      type: string
      enum:
      - CAT_DRAFT
      - CAT_QUOTED
      - CAT_NEW
      - CAT_READY_TO_SIGN
      - CAT_WAITING_FOR_VISA
      - CAT_VISA_REJECTED
      - CAT_DELIVERED
      - CAT_CANCELED
      - CAT_DENIED
      - CAT_CONFIRMED
      - CAT_DONE
      - CAT_WITHDRAWN
      - CAT_VIOLATED
      - CAT_EXECUTED
      - CAT_WITHDRAWN_EXECUTED
  notIn:
    type: array
    items:
      type: string
      enum:
      - CAT_DRAFT
      - CAT_QUOTED
      - CAT_NEW
      - CAT_READY_TO_SIGN
      - CAT_WAITING_FOR_VISA
      - CAT_VISA_REJECTED
      - CAT_DELIVERED
      - CAT_CANCELED
      - CAT_DENIED
      - CAT_CONFIRMED
      - CAT_DONE
      - CAT_WITHDRAWN
      - CAT_VIOLATED
      - CAT_EXECUTED
      - CAT_WITHDRAWN_EXECUTED
  isNull:
    type: boolean
description: Категория документа
