type: object
properties:
  self:
    type: string
    description: Обязательная ссылка
  first:
    type: string
    description: При передаче большого кол-ва данных используется как URI указатель на элементы в потоке данных
  last:
    type: string
    description: При передаче большого кол-ва данных используется как URI указатель на элементы в потоке данных
  prev:
    type: string
    description: При передаче большого кол-ва данных используется как URI указатель на элементы в потоке данных
  next:
    type: string
    description: При передаче большого кол-ва данных используется как URI указатель на элементы в потоке данных
description: Объект для указания ссылок
