type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - GPB
    - VTB
    - SBR
  notEq:
    type: string
    enum:
    - GPB
    - VTB
    - SBR
  in:
    type: array
    items:
      type: string
      enum:
      - GPB
      - VTB
      - SBR
  notIn:
    type: array
    items:
      type: string
      enum:
      - GPB
      - VTB
      - SBR
  isNull:
    type: boolean
description: Код банка-партнера (in)
