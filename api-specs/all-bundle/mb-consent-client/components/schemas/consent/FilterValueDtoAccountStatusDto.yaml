type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - ENABLED
    - DISABLED
    - DELETED
  notEq:
    type: string
    enum:
    - ENABLED
    - DISABLED
    - DELETED
  in:
    type: array
    items:
      type: string
      enum:
      - ENABLED
      - DISABLED
      - DELETED
  notIn:
    type: array
    items:
      type: string
      enum:
      - ENABLED
      - DISABLED
      - DELETED
  isNull:
    type: boolean
  gt:
    type: string
    enum:
    - ENABLED
    - DISABLED
    - DELETED
  ge:
    type: string
    enum:
    - ENABLED
    - DISABLED
    - DELETED
  lt:
    type: string
    enum:
    - ENABLED
    - DISABLED
    - DELETED
  le:
    type: string
    enum:
    - ENABLED
    - DISABLED
    - DELETED
description: Статус счета (eq)
