type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - PASSWORD
    - PIN
  notEq:
    type: string
    enum:
    - PASSWORD
    - PIN
  in:
    type: array
    items:
      type: string
      enum:
      - PASSWORD
      - PIN
  notIn:
    type: array
    items:
      type: string
      enum:
      - PASSWORD
      - PIN
  isNull:
    type: boolean
