type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - WORK
    - SUSPENDED
    - TERMINATED
    - NOT_WORK
  notEq:
    type: string
    enum:
    - WORK
    - SUSPENDED
    - TERMINATED
    - NOT_WORK
  in:
    type: array
    items:
      type: string
      enum:
      - WORK
      - SUSPENDED
      - TERMINATED
      - NOT_WORK
  notIn:
    type: array
    items:
      type: string
      enum:
      - WORK
      - SUSPENDED
      - TERMINATED
      - NOT_WORK
  isNull:
    type: boolean
description: Статус действия УЦ. Полное совпадение
