type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - NEW
    - PROCESSING
    - ACTIVE
    - REJECTED
  notEq:
    type: string
    enum:
    - NEW
    - PROCESSING
    - ACTIVE
    - REJECTED
  in:
    type: array
    items:
      type: string
      enum:
      - NEW
      - PROCESSING
      - ACTIVE
      - REJECTED
  notIn:
    type: array
    items:
      type: string
      enum:
      - NEW
      - PROCESSING
      - ACTIVE
      - REJECTED
  isNull:
    type: boolean
description: Статус СПБ. Полное совпадение.
