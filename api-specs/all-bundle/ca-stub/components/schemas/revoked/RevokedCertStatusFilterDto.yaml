type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    description: Статус отзыва сертификата
    enum:
    - DRAFT
    - REVOKED
  notEq:
    type: string
    description: Статус отзыва сертификата
    enum:
    - DRAFT
    - REVOKED
  in:
    type: array
    items:
      type: string
      description: Статус отзыва сертификата
      enum:
      - DRAFT
      - REVOKED
  notIn:
    type: array
    items:
      type: string
      description: Статус отзыва сертификата
      enum:
      - DRAFT
      - REVOKED
  isNull:
    type: boolean
description: Фильтр по статусу отзыва сертификата
