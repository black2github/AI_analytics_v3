type: object
properties:
  amount:
    type: string
    description: Текущее значение объёма учитываемых операций в рублях без копеек, если код вида суммы предполагает подсчёт суммы операций
    nullable: true
  count:
    type: string
    description: Текущее значение количества учитываемых операций, если код вида суммы предполагает подсчёт количества операций
    nullable: true
description: Текущее значение суммы по счётчику
