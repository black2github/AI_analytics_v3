type: object
properties:
  contains:
    type: string
  eq:
    type: string
    enum:
    - ACTIVE
    - BLOCKED
    - BLOCKED_BANK
    - BLOCKED_EXCEED_PIN
    - BLOCKED_USER
    - EXPIRED
    - AWAITING_ACTIVATION
    - INACTIVE
    - OTHER
    - PICKUP_READY
    - REISSUING
    - CLOSING
    - CLOSED
  in:
    type: array
    items:
      type: string
      enum:
      - ACTIVE
      - BLOCKED
      - BLOCKED_BANK
      - BLOCKED_EXCEED_PIN
      - BLOCKED_USER
      - EXPIRED
      - AWAITING_ACTIVATION
      - INACTIVE
      - OTHER
      - PICKUP_READY
      - REISSUING
      - CLOSING
      - CLOSED
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    enum:
    - ACTIVE
    - BLOCKED
    - BLOCKED_BANK
    - BLOCKED_EXCEED_PIN
    - BLOCKED_USER
    - EXPIRED
    - AWAITING_ACTIVATION
    - INACTIVE
    - OTHER
    - PICKUP_READY
    - REISSUING
    - CLOSING
    - CLOSED
  notIn:
    type: array
    items:
      type: string
      enum:
      - ACTIVE
      - BLOCKED
      - BLOCKED_BANK
      - BLOCKED_EXCEED_PIN
      - BLOCKED_USER
      - EXPIRED
      - AWAITING_ACTIVATION
      - INACTIVE
      - OTHER
      - PICKUP_READY
      - REISSUING
      - CLOSING
      - CLOSED
description: Статус карты (in)
