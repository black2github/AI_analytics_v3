type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - ConsentList
    - ConsentManagement
  notEq:
    type: string
    enum:
    - ConsentList
    - ConsentManagement
  in:
    type: array
    items:
      type: string
      enum:
      - ConsentList
      - ConsentManagement
  notIn:
    type: array
    items:
      type: string
      enum:
      - ConsentList
      - ConsentManagement
  isNull:
    type: boolean
