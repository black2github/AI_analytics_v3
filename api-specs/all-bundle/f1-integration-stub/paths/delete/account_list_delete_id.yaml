delete:
  summary: Удаление записи в таблице GPS_ECO_LIST_ACCS
  tags:
  - Обновление заглушек GPS_ECO_LIST_ACCS (Счета клиента в Ф1)
  parameters:
  - name: id
    in: path
    required: true
    schema:
      $ref: ../../components/schemas/common/UUID.yaml
  responses:
    '204':
      description: No Content
