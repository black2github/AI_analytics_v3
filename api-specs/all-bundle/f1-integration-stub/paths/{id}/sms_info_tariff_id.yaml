get:
  summary: Получение записи из таблицы gps_eco_accs_smsinfo по идентификатору
  tags:
  - Обновление заглушек GPS_ECO_SMS_INFO_ACCS (счет и тариф для списания комиссии (смс-информирование))
  parameters:
  - name: id
    in: path
    required: true
    schema:
      $ref: ../../components/schemas/common/UUID.yaml
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/common/AccountSmsInfoTariffDto.yaml
