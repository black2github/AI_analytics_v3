type: object
properties:
  contains:
    type: string
  eq:
    type: string
    description: Действие по изменению статуса карты
    enum:
    - HARD_BLOCK
    - CLOSE
    - SOFT_BLOCK
    - UNLOCK
  in:
    type: array
    items:
      type: string
      description: Действие по изменению статуса карты
      enum:
      - HARD_BLOCK
      - CLOSE
      - SOFT_BLOCK
      - UNLOCK
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    description: Действие по изменению статуса карты
    enum:
    - HARD_BLOCK
    - CLOSE
    - SOFT_BLOCK
    - UNLOCK
  notIn:
    type: array
    items:
      type: string
      description: Действие по изменению статуса карты
      enum:
      - HARD_BLOCK
      - CLOSE
      - SOFT_BLOCK
      - UNLOCK
description: Действие
