type: object
properties:
  contains:
    type: string
  eq:
    type: string
    enum:
    - LOSS_CARD
    - LOSS_PIN
    - COMPROMISE_CARD
    - FINISH
    - OTHER
  in:
    type: array
    items:
      type: string
      enum:
      - LOSS_CARD
      - LOSS_PIN
      - COMPROMISE_CARD
      - FINISH
      - OTHER
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    enum:
    - LOSS_CARD
    - LOSS_PIN
    - COMPROMISE_CARD
    - FINISH
    - OTHER
  notIn:
    type: array
    items:
      type: string
      enum:
      - LOSS_CARD
      - LOSS_PIN
      - COMPROMISE_CARD
      - FINISH
      - OTHER
description: Причина блокировки
