type: object
properties:
  contains:
    type: string
  eq:
    type: string
    enum:
    - NO_CHANGE
    - CHANGE
    - DELETE
  in:
    type: array
    items:
      type: string
      enum:
      - NO_CHANGE
      - CHANGE
      - DELETE
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    enum:
    - NO_CHANGE
    - CHANGE
    - DELETE
  notIn:
    type: array
    items:
      type: string
      enum:
      - NO_CHANGE
      - CHANGE
      - DELETE
description: Месячный лимит (eq)
