type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - IMPORTED
    - SIGN_ERROR
    - IMPORTED_FILE_ERROR
    - SIGNED
    - NEW
    - READ
    - CANCELED
  notEq:
    type: string
    enum:
    - IMPORTED
    - SIGN_ERROR
    - IMPORTED_FILE_ERROR
    - SIGNED
    - NEW
    - READ
    - CANCELED
  in:
    type: array
    items:
      type: string
      enum:
      - IMPORTED
      - SIGN_ERROR
      - IMPORTED_FILE_ERROR
      - SIGNED
      - NEW
      - READ
      - CANCELED
  notIn:
    type: array
    items:
      type: string
      enum:
      - IMPORTED
      - SIGN_ERROR
      - IMPORTED_FILE_ERROR
      - SIGNED
      - NEW
      - READ
      - CANCELED
  isNull:
    type: boolean
description: Фильтр по статусам
