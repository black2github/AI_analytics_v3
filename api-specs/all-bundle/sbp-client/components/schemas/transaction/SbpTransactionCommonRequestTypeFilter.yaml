type: object
description: Фильтр по типу обобщенного заявления на платеж СБП
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    description: Тип операции в обобщенном заявлении на платеж СБП
    enum:
    - TRANSFER
    - RETURN
  notEq:
    type: string
    description: Тип операции в обобщенном заявлении на платеж СБП
    enum:
    - TRANSFER
    - RETURN
  in:
    type: array
    items:
      type: string
      description: Тип операции в обобщенном заявлении на платеж СБП
      enum:
      - TRANSFER
      - RETURN
  notIn:
    type: array
    items:
      type: string
      description: Тип операции в обобщенном заявлении на платеж СБП
      enum:
      - TRANSFER
      - RETURN
  isNull:
    type: boolean
