type: object
properties:
  contains:
    type: string
  eq:
    type: string
    enum:
    - DRAFT
    - ACTIVE
    - DELETED
  in:
    type: array
    items:
      type: string
      enum:
      - DRAFT
      - ACTIVE
      - DELETED
  isNull:
    type: boolean
  notContains:
    type: string
  notEq:
    type: string
    enum:
    - DRAFT
    - ACTIVE
    - DELETED
  notIn:
    type: array
    items:
      type: string
      enum:
      - DRAFT
      - ACTIVE
      - DELETED
description: Фильтрация по статусу (eq)
