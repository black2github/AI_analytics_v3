type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    description: Статус заявки фронт офиса на открытие первого счета.
    enum:
    - NONE
    - NEW
    - EXPORT_TO_ESK
    - EXPORTED_TO_ESK
    - ACCEPTED_BY_ESK
    - EXPORT_TO_ESK_ERROR
    - EXECUTED_BY_ESK
    - EXPORT_TO_TESSA
    - EXPORT_TO_TESSA_ERROR
    - EXECUTED_BY_TESSA
    - WAITING_FOR_ESK_DECISION
    - APPROVED
    - WAITING_FOR_ESK_ACCOUNTS
    - EXECUTED
    - REJECTED
    - REFUSED
    - EXPORT_TO_ESK_OR_EXPORT_TO_ESK_ERROR
    - EXPORT_TO_TESSA_OR_EXPORT_TO_TESSA_ERROR
    - EXPORT_TO_ESK_ERROR_OR_REJECTED
  notEq:
    type: string
    description: Статус заявки фронт офиса на открытие первого счета.
    enum:
    - NONE
    - NEW
    - EXPORT_TO_ESK
    - EXPORTED_TO_ESK
    - ACCEPTED_BY_ESK
    - EXPORT_TO_ESK_ERROR
    - EXECUTED_BY_ESK
    - EXPORT_TO_TESSA
    - EXPORT_TO_TESSA_ERROR
    - EXECUTED_BY_TESSA
    - WAITING_FOR_ESK_DECISION
    - APPROVED
    - WAITING_FOR_ESK_ACCOUNTS
    - EXECUTED
    - REJECTED
    - REFUSED
    - EXPORT_TO_ESK_OR_EXPORT_TO_ESK_ERROR
    - EXPORT_TO_TESSA_OR_EXPORT_TO_TESSA_ERROR
    - EXPORT_TO_ESK_ERROR_OR_REJECTED
  in:
    type: array
    items:
      type: string
      description: Статус заявки фронт офиса на открытие первого счета.
      enum:
      - NONE
      - NEW
      - EXPORT_TO_ESK
      - EXPORTED_TO_ESK
      - ACCEPTED_BY_ESK
      - EXPORT_TO_ESK_ERROR
      - EXECUTED_BY_ESK
      - EXPORT_TO_TESSA
      - EXPORT_TO_TESSA_ERROR
      - EXECUTED_BY_TESSA
      - WAITING_FOR_ESK_DECISION
      - APPROVED
      - WAITING_FOR_ESK_ACCOUNTS
      - EXECUTED
      - REJECTED
      - REFUSED
      - EXPORT_TO_ESK_OR_EXPORT_TO_ESK_ERROR
      - EXPORT_TO_TESSA_OR_EXPORT_TO_TESSA_ERROR
      - EXPORT_TO_ESK_ERROR_OR_REJECTED
  notIn:
    type: array
    items:
      type: string
      description: Статус заявки фронт офиса на открытие первого счета.
      enum:
      - NONE
      - NEW
      - EXPORT_TO_ESK
      - EXPORTED_TO_ESK
      - ACCEPTED_BY_ESK
      - EXPORT_TO_ESK_ERROR
      - EXECUTED_BY_ESK
      - EXPORT_TO_TESSA
      - EXPORT_TO_TESSA_ERROR
      - EXECUTED_BY_TESSA
      - WAITING_FOR_ESK_DECISION
      - APPROVED
      - WAITING_FOR_ESK_ACCOUNTS
      - EXECUTED
      - REJECTED
      - REFUSED
      - EXPORT_TO_ESK_OR_EXPORT_TO_ESK_ERROR
      - EXPORT_TO_TESSA_OR_EXPORT_TO_TESSA_ERROR
      - EXPORT_TO_ESK_ERROR_OR_REJECTED
  isNull:
    type: boolean
description: Фильтр для статусов технических заявок на открытие первого счёта в офисе банка
