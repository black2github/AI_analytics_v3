type: object
properties:
  field:
    type: string
    description: Поле
  direction:
    type: string
    description: Порядок сортировки
    enum:
    - ASC
    - DESC
  nullHandling:
    type: string
    description: Стратегия сортировки null значений
    enum:
    - NULLS_FIRST
    - NULLS_LAST
required:
- field
