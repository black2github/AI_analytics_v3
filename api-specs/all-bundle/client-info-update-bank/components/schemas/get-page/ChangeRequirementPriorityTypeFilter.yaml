type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - CRITICAL
    - UNCRITICAL
  notEq:
    type: string
    enum:
    - CRITICAL
    - UNCRITICAL
  in:
    type: array
    items:
      type: string
      enum:
      - CRITICAL
      - UNCRITICAL
  notIn:
    type: array
    items:
      type: string
      enum:
      - CRITICAL
      - UNCRITICAL
  isNull:
    type: boolean
description: Критичность. Полное совпадение.
