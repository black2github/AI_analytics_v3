type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - CLIENT_AND_USERS
    - CLIENT
    - USER
  notEq:
    type: string
    enum:
    - CLIENT_AND_USERS
    - CLIENT
    - USER
  in:
    type: array
    items:
      type: string
      enum:
      - CLIENT_AND_USERS
      - CLIENT
      - USER
  notIn:
    type: array
    items:
      type: string
      enum:
      - CLIENT_AND_USERS
      - CLIENT
      - USER
  isNull:
    type: boolean
description: Код типа субъекта из 'Типы субъектов обновления сведений'. Полное совпадение. Мультивыбор.
