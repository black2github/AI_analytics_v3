type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - NONE
    - NEW
    - DRAFT
    - DELETED
    - SIGNED
    - DELIVERED
    - DETAILS_VALID
    - SIGN_VALID
    - RECEIVED
    - EXPORT_TO_ESK
    - EXPORTED_TO_ESK
    - ACCEPTED_BY_ESK
    - EXPORT_TO_ESK_ERROR
    - EXECUTED_BY_ESK
    - EXPORT_TO_TESSA
    - EXPORT_TO_TESSA_ERROR
    - EXECUTED_BY_TESSA
    - WAITING_FOR_ESK_DECISION
    - EXECUTED
    - REJECTED
  notEq:
    type: string
    enum:
    - NONE
    - NEW
    - DRAFT
    - DELETED
    - SIGNED
    - DELIVERED
    - DETAILS_VALID
    - SIGN_VALID
    - RECEIVED
    - EXPORT_TO_ESK
    - EXPORTED_TO_ESK
    - ACCEPTED_BY_ESK
    - EXPORT_TO_ESK_ERROR
    - EXECUTED_BY_ESK
    - EXPORT_TO_TESSA
    - EXPORT_TO_TESSA_ERROR
    - EXECUTED_BY_TESSA
    - WAITING_FOR_ESK_DECISION
    - EXECUTED
    - REJECTED
  in:
    type: array
    items:
      type: string
      enum:
      - NONE
      - NEW
      - DRAFT
      - DELETED
      - SIGNED
      - DELIVERED
      - DETAILS_VALID
      - SIGN_VALID
      - RECEIVED
      - EXPORT_TO_ESK
      - EXPORTED_TO_ESK
      - ACCEPTED_BY_ESK
      - EXPORT_TO_ESK_ERROR
      - EXECUTED_BY_ESK
      - EXPORT_TO_TESSA
      - EXPORT_TO_TESSA_ERROR
      - EXECUTED_BY_TESSA
      - WAITING_FOR_ESK_DECISION
      - EXECUTED
      - REJECTED
  notIn:
    type: array
    items:
      type: string
      enum:
      - NONE
      - NEW
      - DRAFT
      - DELETED
      - SIGNED
      - DELIVERED
      - DETAILS_VALID
      - SIGN_VALID
      - RECEIVED
      - EXPORT_TO_ESK
      - EXPORTED_TO_ESK
      - ACCEPTED_BY_ESK
      - EXPORT_TO_ESK_ERROR
      - EXECUTED_BY_ESK
      - EXPORT_TO_TESSA
      - EXPORT_TO_TESSA_ERROR
      - EXECUTED_BY_TESSA
      - WAITING_FOR_ESK_DECISION
      - EXECUTED
      - REJECTED
  isNull:
    type: boolean
description: Статус документа. Полное совпадение. Мультивыбор.
