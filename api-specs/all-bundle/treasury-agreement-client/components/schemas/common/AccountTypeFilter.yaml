type: object
properties:
  contains:
    type: string
  notContains:
    type: string
  eq:
    type: string
    enum:
    - EXTERNAL
    - INTERNAL
  notEq:
    type: string
    enum:
    - EXTERNAL
    - INTERNAL
  in:
    type: array
    items:
      type: string
      enum:
      - EXTERNAL
      - INTERNAL
  notIn:
    type: array
    items:
      type: string
      enum:
      - EXTERNAL
      - INTERNAL
  isNull:
    type: boolean
