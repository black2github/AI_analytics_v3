type: object
properties:
  contains:
    type: string
  eq:
    type: string
    enum:
    - PDF
    - XLS
  ge:
    type: string
    enum:
    - PDF
    - XLS
  gt:
    type: string
    enum:
    - PDF
    - XLS
  in:
    type: array
    items:
      type: string
      enum:
      - PDF
      - XLS
  isNull:
    type: boolean
  le:
    type: string
    enum:
    - PDF
    - XLS
  lt:
    type: string
    enum:
    - PDF
    - XLS
  notContains:
    type: string
  notEq:
    type: string
    enum:
    - PDF
    - XLS
  notIn:
    type: array
    items:
      type: string
      enum:
      - PDF
      - XLS
description: Формат отчета (eq)
