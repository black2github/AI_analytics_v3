
# Миграция для L4 процессора (Google Colab)

Позволяет мигрировать с одновекторного хранилища. Позволяет и создавать новое и вливать в существующее
новые данные (например, добавлять интеграции ПОСЛЕ того, как влиты все сервисы).

**Ячейка 1 — Установка зависимостей**

```python
!pip install -q chromadb langchain-chroma langchain-core langchain-huggingface \
    sentence-transformers langchain-text-splitters python-dotenv anyio
```

**Ячейка 2 — Загрузка source хранилища**

```python
# загрузка chroma_db_source.tar.gz
# браузером (долго)
#from google.colab import files
# появляется кнока загрузки
#uploaded = files.upload()

# через Google Drive (быстро)
from google.colab import drive
drive.mount('/content/drive')

# Укажи путь к файлам на твоём Google Drive
DRIVE_PATH = "/content/drive/MyDrive/ГПБ/RAG-сервис/"
#
!cp "{DRIVE_PATH}/chroma_db_src.tar.gz" /content/ # копируем хранилище-источник
#!cp "{DRIVE_PATH}/chroma_db_dst.tar.gz" /content/ # копируем хранилище-приемник
!ls -lh /content/*.tar.gz
```

**Ячейка 3 — Распаковка source**

```python
!tar -xzf chroma_db_src.tar.gz
print("Источник документов")
!ls ./store/chroma_db_CC/
print("Приемник документов")
#!tar -xzf chroma_db_dst.tar.gz
#!ls ./store/chroma_db2/
```

**Ячейка 4 — Загрузка проекта**

```python
# Загружаем архив с кодом проекта
#uploaded = files.upload()  # загружаем project.tar.gz
!cp "{DRIVE_PATH}/project.tar.gz" /content/ # копируем исходники
!cp "{DRIVE_PATH}/summaries.json" /content/ # копируем summary для страниц
!ls -lh /content/*.tar.gz
!tar -xzf project.tar.gz
!ls app/
!ls ./
```

**Ячейка 5 — Создание .env**

```python
env_content = """
EMBEDDING_PROVIDER=huggingface
EMBEDDING_MODEL=deepvk/USER2-base
EMBEDDING_MODELS_WITH_PREFIXES=deepvk/USER2-base
EMBEDDING_DEVICE=cuda
EMBEDDING_MAX_SEQ_LENGTH=512
EMBEDDING_MAX_SEQ_LENGTH_TITLE=128
EMBEDDING_MAX_SEQ_LENGTH_SUMMARY=512
EMBEDDING_MAX_SEQ_LENGTH_CONTENT=8192
EMBEDDING_BATCH_SIZE_TITLE=256
EMBEDDING_BATCH_SIZE_SUMMARY=64
EMBEDDING_BATCH_SIZE_CONTENT=3
UNIFIED_STORAGE_NAME=unified_requirements
CHUNK_MODE=none
CHUNK_MAX_PAGE_SIZE=15000
CHUNK_SIZE=1500
CHUNK_OVERLAP=200
MV_TITLE_WEIGHT=0.7
MV_SUMMARY_WEIGHT=0.25
MV_CONTENT_WEIGHT=0.05
CONTEXT_INTEGRATION_TOP_K=10
INDEXING_MODE=multi_vector
"""

with open(".env", "w") as f:
    f.write(env_content.strip())

print("OK")
```

**Ячейка 6 — Снятие offline-блокировки и проверка GPU**

```python
import os
os.environ["HF_HUB_OFFLINE"] = "0"
os.environ["TRANSFORMERS_OFFLINE"] = "0"
os.environ["PYTORCH_CUDA_ALLOC_CONF"] = "expandable_segments:True"

import torch
print("CUDA available:", torch.cuda.is_available())
if torch.cuda.is_available():
    print("GPU:", torch.cuda.get_device_name(0))
    print("VRAM:", round(torch.cuda.get_device_properties(0).total_memory / 1e9, 1), "GB")
```

**Ячейка 7 — Запуск миграции**

```python
!PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \
  HF_HUB_OFFLINE=0 TRANSFORMERS_OFFLINE=0 \
  EMBEDDING_DEVICE=cuda \
  PYTHONPATH=/content \
  python app/scripts/migrate_to_multi_vector.py \
    --all-services \
    --source-dir ./store/chroma_db \
    --target-dir ./store/chroma_db2 \
    --summaries-file ./summaries.json \
    --force
```

**Ячейка 8 — Немедленное архивирование и скачивание (запускать сразу после завершения ячейки 7)**

```python
import os

if os.path.exists("./store/chroma_db2"):
    !tar -czf /content/chroma_db2.tar.gz -C /content/store chroma_db2
    size = os.path.getsize("/content/chroma_db2.tar.gz")
    print(f"Archive size: {size / 1e9:.2f} GB")

    if size > 1000:
        #from google.colab import files
        #files.download("/content/chroma_db2.tar.gz")
        #print("Download started.")
        # вариант копирования через Google Drive
        from google.colab import drive
        #drive.mount('/content/drive')
        # Укажи путь к файлам на твоём Google Drive
        #DRIVE_PATH = "/content/drive/MyDrive/ГПБ/RAG-сервис/" 
        !cp /content/chroma_db2.tar.gz "{DRIVE_PATH}" 
        print("Хранилище скопировано.")
    else:
        print("ERROR: archive is empty, do not download")
else:
    print("ERROR: chroma_db2 not found")
```
