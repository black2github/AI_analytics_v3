# Протокол прогона скиллов на эталоне O2+ (клон, без push)

Дата старта: 2026-08-20. Площадка: `C:\doc-as-code\etalon\docs-o2new`
(клон docs-o2new, master; коммиты локальные, push не делается).
Исполнитель: Cursor, **Grok 4.6 (режим High)** — решение аналитика:
модель сильная, фиксируется до конца генерации; сопоставимость со
слабой моделью inkasso-run1 не является целью прогона.

## Вводные

- Вход (только чтение): `sources\confluence\o2plus` (210 файлов; в
  выгрузке 99 страниц с page_id).
- Выход: `output\o2plus\docs\` — новый каталог; существующие
  `output\open-add-account` (старый эталон, ✗38) и `output\inquiry`
  не трогаются до финальной замены.
- Канон: `_meta` клона заменён срезом фазы 4 (`meta/phase4-scope-rule`
  4a9c87c, методологический срез 3a6e094) — подготовительный коммит
  клона **8d81921**; `sandbox/` добавлен в `.gitignore`.

## Решения аналитика

- **2026-08-20, генераторы разрешены**: скрипты, создающие файлы
  комплекта (`sandbox/build_data_model.py` и т.п.), в ЭТОМ прогоне
  легальны (сильная модель; сторожа значений — контроль). Отличие от
  правила inkasso-run1 «запрет самодельных скриптов правок» —
  осознанное; урок К-33 остаётся в силе: повреждения от скриптов не
  всегда видимы сторожам, сверка эксперта обязательна.
- **2026-08-20, идентификатор сервиса**: `service: open-add-account`
  (канонический код из `_meta/services.json`), НЕ `o2plus` из вводных
  захода 1. Каталог `output\o2plus` остаётся рабочей площадкой прогона
  (путь ≠ service-id временно), физическая замена старого эталона —
  финальным заходом. Закрывает OQ-003 комплекта.
- **2026-08-20, формат записей open-questions** — по канону
  conventions §6: `## OQ-NNN. <формулировка>` + Статус / Затронутые
  документы / Суть / Решение. Агентский формат захода 1
  (Вопрос/Предположение/Что проверить/Рекомендация) выравнивается
  дозаходом.
- **2026-08-20, модель CALL (целевая структура для руководителя)** —
  закрывает пункт «в проработке» conventions §5.1.3: каталог
  `contract-call/` (ед. число) хранит карточки вызова ЛЮБЫХ чужих
  контрактов единым префиксом `CALL-NNN` (реестр `CALL-000`); базовый
  контракт живёт у владельца: внешняя АС — docs-external-contracts
  (`EXTINT-<AS>-NNN`), чужой сервис Экосистемы — его
  `internal-contract/` (`INTC-NNN`), платформенный метод СО
  спецификацией — документация платформы. Платформенные функции БЕЗ
  опубликованной спецификации остаются PLT-записями в
  platform-functions.md (граница — наличие спецификации). Следствия
  для волны канона: схема ID §5 (префикс CALL, судьба usage
  `EXTINT-NNN`), §5.1.1/5.1.3, переименование каталогов в ед. число
  (contract-call, internal-contract, notification, agent) — скиллы
  сейчас создают во множественном. README-заготовки целевой
  структуры — `C:\tmp\readme-заготовки\`.

## Заход 1 — data-model (14 стр. + корневая). ПРИНЯТ С ДОЗАХОДОМ

Сдача: OK со второй итерации (✓ 10, ✗ 0, ⚠ 1); перегон эксперта
подтвердил. Долги 15, OQ 14 (append-only ✓). Комплект: README (DM-000),
dictionaries (DM-001), ENT-001…ENT-008, матрица, open-questions.

Хронометраж: журнал фиксирует только правки после первого селфчека
(00:39:46 БРАК → 00:42:38 OK, 2 мин 52 с); по mtime от первого следа
на диске ≈15 мин. Причина неполноты: baseline-прогон на старте в
промпт не попал — со следующих заходов в промпте строка
«старт: selfcheck --out sandbox/baseline.txt».

Находки сверки (глаза эксперта, при зелёном вердикте):

1. **ent-001, атрибуты БСК 12.5/12.6 — потеря данных**: описание
   источника «Заполняется при выполнении контроля "Проверка наличия
   ИГК и Счета заказчика в АС БСК" (см. [Контроли заявки]) из метода
   БСК_Проверка ИГК и счета заказчика в АС БСК» сжато до отсылки
   «Контроль «…».» — потеряны правило заполнения и имя
   метода-источника (0 вхождений в комплекте, долга нет). Сторож
   кавычечных литералов погашен минимальной вставкой — подгонка под
   сторож в мягкой форме, сильная модель тоже так делает.
2. **ent-005/007/008 — дубль паспорта**: к слоту шаблона «Таблица БД»
   добавлена жирная строка-лейбл источника «**Название физической
   таблицы БД**». Корень приборный: check_source_tables требовал
   лейбл-ячейки двухколонного паспорта как значения → **К-35**
   (2026-08-20): двухколонная таблица, у которой заголовочная ячейка
   и первая ячейка каждой строки — целиком жирный лейбл, — паспорт
   «лейбл→значение»; лейблы не требуются, значения второй колонки
   сторожатся. Тесты TestK35 (4 шт., включая НЕсрабатывание);
   синк прибора в оба репо.

Дозаход 1.1 (задачи): восстановить описания БСК-атрибутов дословно
(+долги ссылок на «Контроли заявки» и метод БСК); убрать
строки-дубли паспорта в ent-005/007/008; `service: open-add-account`
во всех файлах + закрыть OQ-003; open-questions.md → формат §6.

Дозаход 1.1 ПРИНЯТ (2026-08-20, версия канона 99bd4ab): все четыре
задачи закрыты с первого захода, перегон эксперта подтвердил
(✓ 10, ✗ 0, ⚠ 1; долгов 17 без просрочек; OQ 001–016 append-only,
формат §6; OQ-015/016 — механические долги на будущие заходы
create-controls / create-external-integrations). Наблюдение прогона:
сильная модель (Grok 4.6 High) сняла класс грубых потерь слабой, но
класс «минимальное гашение сторожа» воспроизвела (БСК-описание) —
он порождается стимулами гейта, не силой модели; data-model — самый
простой тип, вывод не переносить на функции/ЭФ без проверки.
