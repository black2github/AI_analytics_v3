# Протокол фазы 4 (доводка единых скиллов) — сервис file-storage, прогон 1

Роль: эксперт-аналитик (план работ, разд. 4). Метод: предсказания ДО запуска →
прогон → сверка → классификация находок (п. 4.4 + Приложение 3 плана) →
правка ЕДИНЫХ скиллов в каноне `docs-o2new/_meta` (meta-ветками) → перегон.
Специфику сервиса не разбираем — только системные классы.

Создан: 2026-08-10, до первого запуска скиллов.

## Контекст и решения

- Источники: `C:\doc-as-code\EAN\docs-file-storage\sources\confluence\`
  (94 страницы, CriticMarkup-маркеров нет — чистый ПРОМ-состав; confluence = raw).
- Методология: БЕЗ тиража — единый источник `C:\doc-as-code\EAN\docs-o2new\_meta`
  (решение пользователя 2026-08-10: правим в одном месте, видим эффект на всех
  сервисах). Следствие-риск — предсказание П-0 ниже.
- В репо уже есть `output/file-service` (работа Э. Шефера, состав скиллов
  неизвестен) — не трогаем; судьба решается с владельцем. Наш прогон — в ветке.
- Выгрузка без `--tasks`: ПОДТВЕРЖДЕНО лидом — это текущий ПРОМ-срез
  (принято всё реализованное). Конверсия — от него; дельты последующих
  задач будут раскладываться отдельными запросами (diff от apply) по уже
  созданным документам.
- `output/file-service/docs` очищен пользователем (прежний комплект коллеги
  игнорируем; топология репо будет исправлена позже: src-file-storage +
  docs-file-storage).
- Находка В-1 (входной контроль, ДО прогона): `sources/README.md` указывал
  несуществующий путь `raw/file-storage/` как актуальный — агент по скиллу
  (профиль источников читается первым) ушёл бы не туда. Исправлено в ветке
  прогона (c69ce86). Класс для копилки: профиль источников — часть данных
  репо, его валидность нужно проверять до прогона (кандидат в чек-лист
  подготовки сервиса).

## Инвентарь источников → жанры → скиллы → порядок

| # | Раздел | Стр. | Скилл | Порядок |
|---|---|---|---|---|
| 1 | Функции | 12 | create-function | 1-й |
| 2 | Процессы-2 | 14 | create-process | 2-й |
| 3 | Интеграция | 1 | create-external-integrations (+ --check --source) | 3-й |
| 4 | Методы (внутр./внешн. ФС, MFlash) | 57 | create-function (platform) / api-first-* — ЖАНР УТОЧНИТЬ по содержимому | 4-й, делить на подразделы |
| — | МД (3 стр.) + Список типов файлов | 4 | create-data-model | **ВНЕ ПРОГОНА — мораторий Д-11** («до закрытия Д-11 массовая генерация моделей данных для новых сервисов не запускается»); скилл не переработан (reverse с кода), в sources/code лежит zip бэкенда → физмодель гарантирована. Вместо прогона: МД file-storage — материал для ЭСКИЗА логической модели (гибридный путь Д-11), отдельной задачей |
| — | ЭФ/контроли | 0 | — | жанры на этом сервисе не проверяются (бэковый) |

## Предсказания (до запуска)

- **П-0 (следствие «без тиража», уверенность высокая):** относительные пути в
  скиллах (`_meta/tools/…`, `sandbox/…`, `output/<service>/…`) поедут от
  неверного корня при workspace из двух репо. Контрмера в промпте запуска:
  явно указать корень методологии и корень выходов. Если агент всё равно
  путается — системная ошибка класса «условие workspace» (прецедент:
  итерация 6 ЕСК).
- **П-1 (перенесено на эскиз Д-11):** таблицы атрибутов МД — сырой HTML;
  профиль `esk-xml-blocks` не подойдёт (иная шапка) → утилита обязана
  ОТКАЗАТЬ порогом, а не выдать тихий брак; потребуется новый профиль
  (штатный путь --sample). Проверяется при подготовке эскиза, не в прогоне.
- **П-2 (Функции/Методы, высокая):** потеря строк в длинных
  реестрах-таблицах и «таблицах доступности»; сверять ЧИСЛО строк с
  источником (правило скилла) — гейта на это вне интеграций НЕТ (кандидат
  на обобщение --check для всех жанров).
- **П-3 (Методы, средняя):** жанровая неоднозначность «метод = функция или
  API-контракт» — агент может разложить не туда; критерий дома прописан
  нечётко → кандидат в правку скиллов/конвенций.
- **П-4 (Процессы, средняя):** прежние классы create-process (нумерация
  шагов, «Далее», прочерки) — проверить, что волны прошлых прогонов их
  закрыли; рецидив = системная.
- **П-5 (Интеграция, низкая):** конвейер обкатан 12 прогонами; ожидание —
  чисто; профиль таблиц, вероятно, потребуется новый (не ЕСК-макет).
- **П-6 (сквозное, высокая):** frontmatter-поля и структура шаблонов — по
  п. 4.4 плана; grep-инварианты (`ENT-|data-model/|open-questions|source`)
  в base-артефактах; ссылки на sources/Confluence в чистовых документах
  запрещены — проверять грепом.

## Гейты прогона (все = 0 или объяснены)

1. Покрытие: каждая страница выгрузки → артефакт (`source-coverage.md`).
2. Структура: чек-листы шаблонов пройдены; обязательные разделы на месте.
3. Потери таблиц: число строк/колонок против источника (для интеграций —
   механически `--check --source`; для остальных — выборочная сверка).
4. Frontmatter: обязательные поля, без запрещённых.
5. Ссылки: нет битых; в чистовых доках нет ссылок на sources/Confluence.
6. Отчёт `verify-vs-sources` по 2–3 артефактам каждого жанра: ⚠/➕/➖ разобраны.

## Журнал находок

| # | Жанр | Находка | Класс (п.4.4) | Системная? | Серьёзность | Куда правка | Статус |
|---|---|---|---|---|---|---|---|
| С-1 | Функции | Скоуп-джампинг: из задачи «функции» создано 45 файлов (весь BRD, процессы, rbac, api, модель) | добавление типов требований | да (давление гейтов §9 — воспроизводимо на любом сервисе) | Критич. | create-artifact: § «Границы скоупа» | закрыт (волна c1c59ef) |
| С-2 | Функции | Фабрикация BRD: features/UC синтезированы без источников (BRD-слоя в выгрузке нет) | галлюцинации | да | Критич. | там же + create-brd: пометка `origin: reconstructed-from-srs`; порядок узаконен как финальный заход create-brd по готовому SRS | закрыт (c1c59ef) |
| С-3 | Функции | Мораторий Д-11 нарушен: создана data-model (агент дозакрывал ссылки функций) | — | да | Критич. | закрывается С-1; +козырь в обоснование Д-11 | закрыт (c1c59ef) |
| С-4 | Функции | Процессы-заглушки («будет восстановлено…») ради ссылок | пустые разделы | да | Некритич. | закрывается С-1 (долг вместо заглушки) | закрыт (c1c59ef) |
| С-5 | Функции | Служебные скрипты в корне репо (не в sandbox/); повтор с прерванного МД-прогона | — | да | Некритич. | create-artifact: § «Границы скоупа», последний пункт | закрыт (c1c59ef) |

Методологическое решение (пользователь, 2026-08-10): движение «снизу-вверх»
узаконено как задуманный порядок _meta — SRS-жанры по одному, финальный заход
create-brd (reverse-from-SRS ассемблер) по готовому SRS с гейтами
достаточности; реконструированный BRD помечается происхождением (не ловит
исходные ошибки SRS — родство с аргументом Д-11 п. 5).

## Журнал прогонов

- 2026-08-10, жанр 1 «Функции», ветка ab-fs-run1 (f52cc52), волна скиллов
  до c1c59ef (правило скоупа внесено ПОСЛЕ прогона). Итог: сами функции
  чистые — покрытие 9/9 подстраниц, структура/frontmatter по канону,
  0 ссылок на sources, таблицы перенесены (одно подозрение — фрагмент
  «байт» fun-cl-01, на verify-vs-sources); скоуп-джампинг вычищен
  (оставлены 9 карточек + README + OQ-004…006). П-0 не подтвердилось
  (пути методологии агент разрешил верно), П-2 не подтвердилось на
  функциях, П-6 чисто.
- 2026-08-10, прогон 2 «Функции» (контрольный перегон по решению
  пользователя), ветка ab-fs-run2 (a83b749), волна c1c59ef. Итог
  ДВУСТОРОННИЙ: (а) карточки функций 100 % идентичны прогону 1 (полная
  стабильность скилла), скрипт в sandbox ✓, BRD-фабрикация НЕ
  воспроизвелась (главный класс закрыт; попутно вскрыт со-провокатор
  прогона 1 — остатки чужого brd в output, вход прогона 2 чист);
  (б) остаточный джампинг SRS-соседей: data-model (мораторий!), 8
  процессов-заглушек, rbac — специализация («собери вход: процесс…») и
  шаблон переиграли текстовый запрет базового скилла. ВТОРОЕ текстовое
  неисполнение класса → механика: самопроверка скоупа git status
  --porcelain с белым списком путей типа (create-artifact) + дубль границ
  в create-function (волна, коммит 2 — eb1bc5c). Долги ссылок в OQ агент
  также не записал — покрыто той же механикой (удаляемое → цитата-долг).
- 2026-08-10, сверка функций пользователем: находка С-6 — потеря типов/
  кратности/пояснений параметров; корень = ШАБЛОН function (таблица из двух
  колонок «Параметр | Обязательность» — канона нет). Попутно С-7: шаблон
  function в трёх местах велел «завести заглушку» чужих типов — вот кто
  переигрывал правило скоупа в прогонах 1–2 (конфликт указаний). Оба
  закрыты волной, коммит 3 (2cb080d): канон параметров (Параметр | Тип |
  Обяз. | Кратность | Описание, дословно из источника, пустое не выдумывать)
  + три заглушки-инструкции заменены на долг в open-questions. Перегон
  функций под новым шаблоном — после жанра 2 (решение пользователя:
  двигаемся на Процессы).
- 2026-08-10, прогон 3 «Функции» (ab-fs-run3, волна до 2cb080d) —
  АННУЛИРОВАН, выход стёрт. Итог двусторонний: (а) МЕХАНИКА СКОУПА
  СРАБОТАЛА ПОЛНОСТЬЮ — только functions/ + матрица + OQ + sandbox, ноль
  чужих типов и заглушек (класс С-1/С-3/С-4 закрыт доказанно);
  (б) КАНОН ПАРАМЕТРОВ НЕ ДОЕХАЛ: таблицы снова «Параметр | Обязательность»
  — в скрипте агента захардкожена старая шапка. Причина С-9: прогон шёл
  в ТОМ ЖЕ чате Cursor — агент воспроизвёл свою прошлую процедуру из
  контекста, не перечитав обновлённый шаблон с диска. Процедурное правило:
  КАЖДЫЙ ПРОГОН — НОВЫЙ ЧАТ АГЕНТА (в ЕСК-серии соблюдалось само собой).
  Попутно С-8: агент удалил чужой analyst-review-checklist.md — перегиб
  механики скоупа («лишнее удали» применено к до-заходному файлу);
  восстановлен. Волна, коммит 4 (6c135cf): механика уточнена («удаляй
  только созданное/изменённое тобой в заходе») + якорь свежести
  методологии (читать шаблон с диска каждый заход). Ветка run3 сброшена
  к чистому входу 38e009c.
- 2026-08-10, прогон 4 «Функции» (НОВЫЙ чат, ab-fs-run3 → 0e194d8, волна
  до 6c135cf). ЖАНР ПРИНЯТ: канон параметров доехал (9/9 карточек по
  5 колонок, типы/кратности/описания дословно, «массив байтов»
  восстановлен — С-6 закрыт доказанно); скоуп чист повторно; долги ссылок
  в OQ-005 (правило наконец исполнено); чужие файлы целы (С-8 закрыт).
  С-9 (новый чат) подтверждён как корень провала прогона 3. Две
  микронаходки → волна, коммит 5 (500e175): С-10 — functions/README.md
  не создан (create-function его не требовал, а create-brd на него
  полагается; теперь обязателен и в белом списке скоупа); С-11 —
  последняя заглушка-инструкция в СКИЛЛЕ (экран без описания) заменена
  на долг. Остаток жанра: дозаход «создай functions/README» (в том же
  чате прогона 4 допустимо — скилл уже свежий).
- 2026-08-10, дозаход functions/README принят (16c60b0): реестр по трём
  контурам + таблицы доступности. Жанр «Функции» закрыт.
- 2026-08-10, жанр 2 «Процессы» (новый чат). ИНЦИДЕНТ С-12 (процедурный,
  вина эксперта): в промпте осталась устаревшая ветка ab-fs-run2 — агент
  САМ переключил репозиторий на неё и работал там (функции старые →
  пользователь увидел «потерю» параметров в fun-cl-01, глядя на run2);
  плюс агент брал комплект процессов ИЗ MASTER как «опору нумерации»
  (влияние чужого комплекта) и, судя по долгой генерации, ходил в
  сеть/remote. Выход перенесён на линию run3 (98cf259): матрица сшита
  вручную (покрытие функций + покрытие процессов + реестры + долги),
  OQ-007…016 приложены, run2 восстановлен как архив. Правки процедуры
  запуска: в промпте ВСЕГДА актуальная ветка; добавить строки «ветки не
  переключай — при несоответствии остановись и спроси; в master и другие
  ветки не заглядывай; сеть/remote не используй».
  СОДЕРЖАТЕЛЬНО жанр 2 ХОРОШ: покрытие 14/14 (10 процессов + статусная
  модель + 3 обёртки в README), Если/то/иначе жирными, статусы
  MALWARE/STORED на местах, 0 ссылок на sources, долги ссылок в
  OQ-013 и секции матрицы (правило скоупа исполнено), 10 спорных мест —
  качественные семантические находки (перепутанные успех/ошибка PRC-002,
  дыра безопасности PRC-010 — материал лидам). Детальная построчная
  сверка шагов — на verify-vs-sources/пользователя.
- 2026-08-10, жанр 3 «Интеграция» (новый чат, промпт с блоком изоляции,
  ab-fs-run3). ПЕРВЫЙ ПРОГОН БЕЗ ПРОЦЕДУРНЫХ ИНЦИДЕНТОВ: ветка не
  переключалась, сети не было, скоуп идеален (external-integrations/ +
  sandbox + OQ-017…021 + матрица §3.3), в чужой prc-006 агент не полез —
  долг OQ-019. Покрытие 1/1 (источник «Интеграция c CDN» = исходящий
  ФС → EdgeCenter GET public_ips_list). Из-за изоляции base-контракт
  осел черновиком в sandbox/extint-base-draft/cdn/ (агент изобрёл сам —
  поведение правильное, требует узаконивания в скилле). Разбор 6 спорных
  мест агента: №2 base-в-sandbox — системное (узаконить); №6 «Обяз.=[1]»
  — ЛОЖНЫЙ БРАК нашего валидатора --check --source (в источнике у строки
  «Тело ответа» действительно литерал [1] в обязательности; дословность
  против словаря ролей — чинить normalize_tables.py: дословное совпадение
  с ячейкой источника = валидно, + тест на НЕсрабатывание); №3 дыра Gate
  (входящий вызов нашего API внешней АС ≠ usage EXTINT) — методологическое,
  ждёт решения; №1 (авторизация «Уточнить у архитектора») и №4
  (EdgeCenter vs Megafon) — специфика источника, OQ к архитектору;
  №5 (MFlash вне папки «Интеграция») — отдельный заход после жанра
  «Методы». Сверка пользователем: С-13 ЧИТАЕМОСТЬ — usage-карточка в
  output не читалась автономно (паспорт уехал в base-черновик, ссылки
  на внешний репо ещё пустые; впечатление потери данных, хотя перенос
  дословный — сверено построчно). Волна читаемости (внесена, НЕ
  закоммичена): шаблон usage + скилл — обязательный раздел «Назначение»
  (внешняя АС, направление, зачем сервису) + чек-лист; терминология
  скилла «Контур base/usage/both» vs «Режим reverse/forward» (путались
  измерения); карточка EXTINT-001 — «Назначение» + сноска на черновик
  base (OQ-018).

## Чекпоинт 2026-08-10 (разделение обсуждения на две ветки)

- **Жанры:** 1 «Функции» — ЗАКРЫТ (16c60b0); 2 «Процессы» — принят
  содержательно (98cf259), построчная сверка шагов за пользователем;
  3 «Интеграция» — прогон чист, волна читаемости внесена; 4 «Методы»
  (57 стр., жанр уточнить) — не начат.
- **Не закоммичено:**
  - docs-o2new (ветка meta/phase4-scope-rule): правки волны читаемости —
    _meta/templates/external-integrations.md («Назначение» в usage +
    чек-лист), _meta/skills/create-external-integrations.md («Контур»,
    шаг «Назначение»).
  - docs-file-storage (ab-fs-run3, HEAD 98cf259): весь выход жанра 3 —
    external-integrations/ (EXTINT-001 + README), sandbox/extint-base-draft/,
    sandbox/normalized/, OQ-017…021, матрица §3.3 + мои правки карточки
    («Назначение», сноска).
- **Решения в ожидании:** (1) узаконить base-черновик в sandbox при
  недоступном внешнем репо — ОБСУДИТЬ ОТДЕЛЬНО (решение пользователя);
  (2) фикс валидатора [1] — СДЕЛАН (см. ниже); (3) Gate направления —
  сейчас или после лидов; (4) коммиты канона и жанра 3.
- 2026-08-10, фикс ложного БРАК валидатора (ветка скиллов — владелец
  normalize_tables.py). Сделано в каноне app/scripts/CI/normalize_tables.py:
  source_role_literals() собирает дословные значения якорных колонок
  источника ПО РОЛЯМ (md-таблицы + сырые HTML-сетки, шапка = первая
  строка); validate_columns милует значение вне словаря роли, если оно
  дословно есть в колонке ТОЙ ЖЕ роли источника (совпадение «где-то в
  источнике» не считается — гейт против настоящих переписываний [1]→1
  не ослаблен); отчёт помечает «дословно из источника — принято».
  Тесты: +5 в tests/test_normalize_tables.py (помилование; НЕсрабатывание
  без --source; настоящий регресс не милуется; чужая роль не милует;
  сбор литералов) — 66/66 зелёные. Реальный кейс extint-cdn-001: БРАК →
  OK (код 0). Копия _meta/tools синкнута байт-в-байт (CRLF сохранён),
  проверена на том же кейсе. В скилл 2c добавлен запрет подгонять
  авторские литералы под словарь ради гейта. ДЛЯ ВЕТКИ УТИЛИТ: синк
  нормализатора в бандл confluence-tree-exporter + VERSION — за ней.
- **Следующий крупный шаг:** жанр 4 «Методы» (источник
  sources/confluence/.../[Файловый-сервис]-Методы/, жанр artefacta
  уточнить: create-function platform vs api-first) + перенос base
  EXTINT-CDN-001 во внешний репо (делает пользователь — сеть).
- 2026-08-11, конвенции: §84 «заводится заглушка» → долги + карточка-минимум
  своего типа; §174 «Заглушка» → «Карточка-минимум» (только своим заходом);
  §216 PLT «записи — заглушки» → «карточки-минимум» только заходом
  platform-function. Конфликт указаний (класс С-7) в conventions закрыт.
- 2026-08-11, ТРЕК Д-11 (снятие моратория пилотом, вариант А — код не
  источник вовсе). Обоснование: в выгрузке file-storage есть логический
  уровень МД — раздел «[Файловый-сервис]-МД», 3 страницы = 3 сущности
  долгов (Сущность-Файл→ENT-001, Одноразовый-токен→ENT-002,
  Белый-список-адресов→ENT-003), имена таблиц БД на страницах, атрибутный
  состав «Наименование|Тип данных|Обязательность|Уникальность|Описание».
  Волна срезов 1–2 ВНЕСЕНА (не закоммичена): скилл create-data-model —
  Reverse перевёрнут («Код — НЕ источник модели данных», источник —
  раздел МД, нормализатор для HTML-таблиц, сущность без страницы = долг,
  счётная сверка строк); шаблон data-model — блок «Reverse с логического
  уровня (Д-11) — канонический порядок» (маппинг колонок, логические
  типы дословно, NOT NULL/UNIQUE из Обязательность/Уникальность, PK/FK/
  default/физтипы НЕ выводить, физимя только из источника, без него ID
  атрибута не строится — спорное место) + правки чек-листа (код-пункты
  действуют только при коде, явно возвращённом в контур). Следующий:
  прогон агентом (новый чат, изоляция, ab-fs-run3) → сверка → снятие
  долгов ENT в OQ-013/матрице. Открыто: классы OQ (вопрос|долг) +
  скрипт-гейт MR — после лидов.
- 2026-08-11, ВОЛНА «ЛОГИЧЕСКАЯ МОДЕЛЬ ДАННЫХ» (решение пользователя по
  итогам пилота): шаблон data-model переведён с физического на логический
  уровень. Ключевые решения: (1) колонки атрибутов = формату страницы
  источника: Атрибут | Физ. имя | Тип (логический, дословно) | Обяз. |
  Уникальность | Описание; «Ограничения БД» упразднены; (2) ID атрибута =
  ENT-NNN.«Бизнес-название» — единая нотация с conventions §5,
  параллельный физ-ID (ENT-NNN.<физ_имя>) упразднён; «Физ. имя» —
  необязательная привязка к реализации, пустота — норма (снят и мой
  пункт OQ-022 «пустые физимена — спорное место»); переименование
  атрибута — каскадная правка ссылок; (3) явный default постановки —
  дословно в «Описании» (закрыт огрех №5 прогона без спецправила);
  (4) секция «Физическая реализация» → «Привязка к реализации» (только
  таблица БД из постановки); (5) связи и ER — логические: M:N прямо,
  без изобретённой таблицы связи; наследование — подписью/дискриминатором;
  кратность из Обяз./Уникальность; (6) forward: техкаркас (id/version/
  created_at) исключён — зона разработки; нейминг бизнес-названиями;
  (7) поколение: прежние комплекты физического формата (O2+) не
  переконвертируются. Проверка потребителей: матрица трассировки не
  ломается (оперирует ENT-ID и файлами), нотация SRS-ссылок уже
  логическая, ER прогона уже фактически логическая. Скилл
  create-data-model упрощён под перенос «как есть». Следующий: перегон
  комплекта data-model file-storage новым прогоном (тест шаблона).
- 2026-08-11, НЕЧИСТЫЙ ПРОГОН data-model АННУЛИРОВАН: запуск случился в
  гонке с внесением блока «Повторный заход» в create-artifact — какую
  версию скилла читал агент, неизвестно. Выход (комплект в новом
  логическом формате, OQ-022…025, правки OQ-001/003/005/013, матрица)
  стёрт; матрица и OQ возвращены к состоянию «после жанра 3»
  (вчерашний выход прогона №1 при этом утрачен — был перезаписан
  некоммиченным; урок: коммитить проверенные прогоны до следующего
  запуска). Из строки долга матрицы убрано «(мораторий Д-11)» — ложный
  сигнал для агента. Блок «Повторный заход» в create-artifact внесён
  (актуализация, сохранение ID, шаблон с диска, удаление только по
  заданию). Следующий: чистый прогон «создай или актуализируй модель
  данных» по универсальной постановке (без подсказок скиллу) на пустом
  входе — тест логического шаблона; тест повторного захода — отдельным
  дозаходом после сверки.
- 2026-08-11, ВТОРОЙ НЕЧИСТЫЙ ПРОГОН data-model, находка С-14. Агент
  запущен в чате с контекстом ночного прогона: «Извлеку итоговые файлы
  из предыдущего прогона», «Восстанавливаю комплект по проверенному
  черновику» — в sandbox/normalized/prev-run/ появились БАЙТОВЫЕ копии
  удалённых ночных файлов (источник — история редактора или дословное
  воспроизведение из контекста; на диске следов не было — git status
  после чистки был чист). Данные шли НЕ по контуру источник →
  нормализатор → шаблон. С-14 = повторение С-9 в новой форме
  (реставрация ВЫХОДА, а не скрипта); текстовый сторож «структуры из
  памяти диалога не переиспользуй» не держит. Фикс: в универсальный
  промпт добавлен третий блок «Прогон с нуля» (единственный источник —
  sources/ и диск; слова «предыдущий прогон/восстановлю/как в прошлый
  раз» в собственном плане = стоп-сигнал). Следы стёрты повторно
  (data-model/, prev-run/, src-*, матрица и OQ откачены к «после
  жанра 3», OQ-001/005/013 возвращены). Спорные места агента
  содержательно стабильны в обоих прогонах (имя таблицы ENT-003;
  «Содержимое файла»; «Алгоритм метки целостности» — константа?) —
  вернутся при чистом прогоне.
- 2026-08-11, ЧИСТЫЙ прогон data-model (новый чат, промпт из трёх блоков)
  — ПРОЦЕДУРА ЗАЧТЕНА: скоуп чист, реставраций нет (независимость
  доказана делом: «Размер» перенесён полнее ночного — мин/макс дословно;
  своя EXT-нумерация). Формат — логический шаблон целиком: «Привязка к
  реализации» (ENT-003 «—»), 6 колонок, default дословно в «Описании»,
  ID ENT-001…003 сохранены, счёт 25+1=26. Находки сверки: С-15 БРАК —
  колонка «Комментарии» справочника статусов потеряна (в его же
  нормализованном выходе была); С-16 — sandbox tables.md в UTF-16
  (агент гнал вывод нормализатора PowerShell-редиректом >); «Срок
  действия» — молчаливый пересказ на конфликте «дословность vs запрет
  поведения» (потеряно «независимо от статуса обработки»); семантическая
  находка СЗИ/upload_checked прошлых прогонов этим агентом не поднята
  (нестабильность семантических находок — ожидаемо); OQ-022/023 вставлены
  в середину реестра (append-only нигде не был записан). Волна по итогам
  (решения пользователя 1,3,4): скилл create-data-model — справочники
  ЦЕЛИКОМ со всеми колонками + --sidecar вместо >; то же в
  create-external-integrations; шаблон — политика «ВСЕ атрибуты сущности
  из постановки, хранение вне БД — не основание исключать» (решение
  п. 3), правило «поведение из описания — в спорные места, не
  пересказывать молча», чек-лист дополнен; conventions §6 — реестр OQ
  append-only. Ожидает решения: EXT-002 (статусы vs Клиент Банка,
  OQ-023). Дозаход по комплекту — после решения.
- 2026-08-11, ВОЛНА «СПРАВОЧНИК СЕРВИСОВ + FRONTMATTER» (решения
  пользователя). Контекст: правила определения «платформенный vs
  бизнес-сервис» в _meta не существовало — агент решал интуицией
  (термин «платформа» нигде не определён, знание разбросано примерами,
  is_platform источников не использовался). Внесено: (1) справочник
  _meta/rules/services.json — копия app/data/services.json со 102
  сервисами, поле platform → is_platform (единое имя с frontmatter
  источников; актуализация — слиянием, НЕ перекопированием: копия
  расходится с оригиналом), code файлового сервиса fileManager2 →
  file_storage (соответствие service_code выгрузки; сервис ПЛАТФОРМЕННЫЙ);
  (2) conventions §5 frontmatter-схема: is_platform (самоидентификация
  комплекта, из справочника) + confluence_page_ids (ОПЦИОНАЛЬНО, только
  reverse, ВСЕГДА список — смешанный тип строка/список = тихие баги;
  главная страница первой; части одной страницы — одинаковый ID; ID
  не выдумывать); (3) правило происхождения уточнено: «в ТЕКСТЕ
  документов», frontmatter-ключ — исключение; (4) критерий EXT в схеме
  ID: владение данными вне сервиса + is_platform владельца по
  справочнику; не определяется — спорное место; свои перечисления —
  НЕ EXT; (5) грепы-антирегрессии create-brd/create-data-model —
  оговорка про frontmatter; (6) frontmatter-образцы 14 шаблонов
  дополнены обоими полями (19 блоков; extint base — только
  confluence_page_ids, без service/is_platform); (7) create-artifact
  шаг 5 — правило заполнения новых полей. Примеры многостраничных
  документов зафиксированы: файл функции+«Параметры вызова», README
  функций из двух реестров, dictionaries из 4 страниц.
- 2026-08-12, ОТКАТ is_platform из frontmatter (решение пользователя по
  оценке через «Принципы проработки скиллов», п. 4 «единый источник
  истины»): поле выводимо из service + справочника — ручная копия
  выводимого = канал рассинхрона, а выгода призрачна (для выдачи EXT
  агент всё равно ходит в справочник за владельцем ЧУЖИХ данных).
  Удалено: строка схемы + пояснение в conventions (заменено правилом
  «платформенность НЕ дублируется — выводится из service»), 18 строк
  из 14 шаблонов, часть шага 5 create-artifact. ОСТАЛИСЬ: справочник
  services.json (единственный источник истины), критерий EXT в схеме ID,
  confluence_page_ids (первичный факт, не выводим — принципу 4 не
  противоречит). Вскрыта ДВУСМЫСЛЕННОСТЬ семантики EXT: «сущность
  платформы» читается двояко — как абсолютная категория («сущности
  платформенных сервисов», интерпретация пользователя и агента ночного
  прогона) vs относительная роль («чужие данные для документируемого
  комплекта», замысел). Разбор в диалоге; правка формулировки — после
  решения пользователя.
- 2026-08-12, МОДЕЛЬ EXT СОГЛАСОВАНА и внесена: строка EXT схемы ID —
  «внешняя зависимость комплекта; ENT/EXT — роли ОТНОСИТЕЛЬНО комплекта
  (моё/чужое); нумерация EXT локальна потребителю; владелец описывает
  свои данные как ENT/справочник независимо от своей платформенности;
  внешние ссылки — именем и якорем, EXT выдаёт потребитель у себя;
  критерий выдачи — ВЛАДЕНИЕ»; terms.md (EXT и «EXTINT vs EXT»)
  зеркально. Справочник services.json из критерия EXT УБРАН (решение
  пользователя: для выдачи EXT не нужен — критерий владение, не
  платформенность); оставлен справочным реестром экосистемы (строка в
  §2: code=service_code выгрузки, актуализация слиянием) — на будущее
  (самоидентификация комплектов, приоритеты тиража). Следствие модели:
  OQ-023 однозначен — статусы у владельца file-storage EXT не носят,
  снятие EXT-002 идёт в пакет дозахода.
- 2026-08-12, ДОЗАХОД data-model ПРИНЯТ (все 7 пунктов ревью исполнены
  точно): статусы 3 колонки с комментариями дословно и без EXT-ID;
  «Содержимое файла» включён (новая политика); «Срок действия» —
  семантика, поведение в OQ-026; EXT-002 погашен строкой реестра
  «выдан ошибочно, не переиспользовать», каскад якорей хирургический
  (status-model + 5 процессов, только замена якоря); confluence_page_ids
  сверены с источником; OQ упорядочен (дубль OQ-007 → OQ-024, +OQ-025
  СЗИ, +OQ-026 удаление по сроку, OQ-023 закрыт). ПЕРВЫЙ ЧИСТЫЙ ТЕСТ
  блока «Повторный заход» ПРОЙДЕН: актуализация на месте, все ID
  сохранены. ЖАНР «МОДЕЛЬ ДАННЫХ» ЗАКРЫТ (мораторий снят делом).
  Микро (FALSE «Проверен DLP») 2026-08-12: первичное «подтверждаю»
  пользователь ОТОЗВАЛ по роли эксперта (системные ошибки скиллов — наша
  зона, содержательные решения по требованиям — команде). Итог: С-17 —
  правило шаблона «boolean — всегда оба значения» при дыре источника
  (описан только TRUE) провоцировало ФАБРИКАЦИЮ второй ветки; правило
  дополнено («второе состояние не выводить — нейтрально по факту + OQ»);
  в ENT-001 FALSE обоих признаков (download_checked, upload_checked)
  нейтрализован («проверка не зафиксирована»); заведён OQ-027 —
  семантику FALSE определяет владелец требований.
- 2026-08-12, ЦЕНТРАЛИЗАЦИЯ ВЫДАЧИ ID (анализ по «Принципам…» п. 3/4:
  копии-проекции правила в 4 скиллах разной полноты = дрейф; инцидент
  EXT-002 — авансовая выдача упоминанием без реестра). Внесено:
  conventions §5 «Схема ID» — единое правило выдачи (следующий
  свободный в скоупе типа ПО РЕЕСТРУ матрицы; каталог файлов и тексты —
  не источник; погашение строкой-примечанием; ID выдаёт только заход
  своего типа); формулировки в create-function / create-screen-form /
  create-external-integrations / create-artifact заменены ссылками на
  правило; в шаг 6 create-artifact — сверка «дублей нет, авансовых ID
  нет». Утилита-выдатчик (полная инкапсуляция) отложена как вторая
  производная — до второго инцидента класса.
- **Разделение обсуждения (Fork, решение пользователя):** ЭТА линия —
  ветка доводки doc-as-code СКИЛЛОВ (docs-o2new `_meta`, docs-file-storage,
  протокол фазы 4, канон нормализатора). Вторая ветка (форк) — доводка
  УТИЛИТ переноса (confluence-tree-exporter, onix-ft транспорт,
  app/scripts/*, CRITICMARKUP_STATUS.md, версия бандла VERSION).
  Владелец `normalize_tables.py` (логика + тесты, все три копии: канон
  app/scripts/CI/, бандл, _meta/tools) — ветка СКИЛЛОВ; ветка утилит
  только синкает готовое в бандл. Версию бандла двигает только ветка
  утилит.
