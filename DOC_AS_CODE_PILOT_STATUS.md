# Пилот Doc-as-Code: точка возврата (2026-09-10)

Статус-файл для продолжения работы из любой сессии/машины. Память
агента локальна и между ноутбуками НЕ переносится — источник истины
этот файл + git. Новой сессии: «прочитай DOC_AS_CODE_PILOT_STATUS.md,
продолжаем с раздела Очередь».

## Роли

Пользователь — владелец пилота: решения, коммиты/push, акцепты
(`accept_canon.py`), передача промптов агентам. Агент (Claude в этом
репо) — владелец канона: калибровки прибора, шаблоны/промпты/гид,
техприёмка сдач. Гейты CLAUDE.md обязательны (коммит только по
запросу, точечный git add, план перед крупным, вопросы вместо
угадывания).

## Репозитории (клоны по веткам!)

| Репо | Ветка | Роль |
|---|---|---|
| requirements-analyzer-v3 (этот) | main | инструменты (app/scripts/CI), рабочие документы (app/data), тесты |
| docs-o2new (gitlab EAN) | **ext/extension-points** | канон: _meta/ (гид, протокол, шаблоны, скиллы, инструменты) |
| eco-techbook (gitlab ED) | **ext/extension-points** | conventions.md (политика ID, §3.1 подсервисы) |
| docs-account-opening-request | trunk | эталон O2+ |
| src-business-cards (локальный, БЕЗ remote) | master | референс-стенд КК; передаётся копией каталога |
| src-business-cards (gitlab EAS, C:\doc-as-code\EAS\) | **master = ПРОМ** | репозиторий сервиса КК: `sources/raw/` архив, `sources/confluence/` источник, `docs/` пуст; тег `src/PROM`, ветка `future` = снимок apply-all, `src/raw-import` |

На этой машине клоны: C:\doc-as-code\ext-sandbox\<имя>. Инструменты
канона синкаются из app/scripts/CI (запись с newline='\r\n', читать
целевой ДО записи; стенд/канон CRLF, app/data — LF).

## Состояние (что сделано, крупно)

1. STS-03 (экранные формы cc-card-status-change) ЗАКРЫТ: 5 заходов,
   8 страниц, 6 ЭФ, 0 OQ, 0 дозаходов; конвейер воспроизведён промптом
   планировщика без владельца канона в петле промптов.
2. ВЫДАЧА первой команде (inkasso-outgoing): ветки запушены. Весь
   комплект миграции живёт в каноне `_meta/migration/` (с 2026-09-09):
   README-маршрутизатор (выбор трека, порядок чтения), инструкции
   треков `track-manual.md` / `track-coordinator.md`, гид `reference.md`,
   промпты `prompt-plan.md` / `prompt-coordinator.md`. Ручная передача
   файлов отменена, копии в app/data удалены; стенд — копией по запросу.
   Комплект вычитан по шести критериям (решения владельца 2026-09-09):
   пути — только `<src>` и `<клоны>`, остальные производные;
   PowerShell-команды с ожидаемым выводом; ссылки по именам разделов.
3. Промпт координатора (`_meta/migration/prompt-coordinator.md`) — роль-агент трека «скорость с
   рисками»: красные линии, прогноз перед запуском, чек-лист приёмки
   9 пунктов, лестница триажа (сначала легальная доводка + информ. FB,
   стоп только без легального прохода), журнал coordination-log.md.
4. Вычитка склейки: <канон> = корень клона; словарь нормативен в
   протоколе (FB/OQ определены); роли акцептующий/владелец канона;
   §11 формула отмашки; §4 имена отчётов -zNN; §6 «заход только после
   OK+отмашки»; факторизация карточки этапа во всех трёх документах.
5. Риск №2 закрыт (Д-23 реестра): ПРОМ-срез = чистый старт, ledger не
   строим; правила идентичности — вход дельта-процесса. Риск №3:
   метрика = журнал сдач под 3 решения (release/переключение/паспорт),
   индекс зрелости не строим; Д-24 (не-markdown артефакты, приложения
   описи, сторож сирот) — сделан параллельной сессией.
6. ЛИНИЯ v1 промпта плана ЗАМОРОЖЕНА (2026-09-03): нормы v1.2 (repo
   справочника сервисов, долг «у владельца», стоп) портированы в v2.2;
   живая линия одна — v2.2 (коммит c54c94e7).
7. Прибор: калибровки STS-03 внесены (ярлык блока без кавычек, <S>,
   скобочный хвост упоминаний); link_debts принимает суб-ID с точкой
   (ответ коллегам: их дефект — старая версия, не наша ветка).
8. СКЛЕЙКА С CriticMarkup-доводкой ЗАКРЫТА (обе сессии, детали —
   CRITICMARKUP_STATUS.md): канал «заморозка поддерева → миграция»
   сквозной: frontmatter `unapproved_jira:<JIRA-ID>` (ручной
   `--flag-page` или forced-путь/сторож экспортёра; номера задач нигде
   не зашиты) → маркер `frozen:<ID>` в req_type описи
   (source_inventory, 846dcc75, синк в канон) → v2.2 шаг 1: «вне SRS:
   заморожено до задачи» одной строкой на задачу, без карточек и
   вопросов; наполнение придёт дельтой по page_id. `reject-all`
   остался только снимающим. Инвариант до уровня 1 (строки истории
   на замороженных страницах Confluence): `--flag-page` обязателен
   при лечении КАЖДОЙ выгрузки.
9. МОДЕЛЬ ВЕТОК РЕПОЗИТОРИЯ СЕРВИСА (решение владельца 2026-09-10,
   выполнено на EAS/src-business-cards): `master` = ПРОМ (force-push на
   бывший тег src/PROM), бывшая хронология 148 срезов → ветка `future`,
   теги src/<ID> хронологии удалены; корневая страница [КК] возвращена в
   raw/ и confluence/; migration-* выведены из confluence/.
   apply_history (6af8aa6b, бандл пересобран): migration-* не
   копируются в целевой каталог, сообщение «Ввод в эксплуатацию: X»,
   ключ --commit-prefix. ИНВАРИАНТ: промпты о ветках/тегах не знают —
   prompt-plan п.3 = «актуальная основная ветка master»; модель веток
   в одном месте: гид reference.md, раздел «Ветки и теги репозитория
   сервиса» (таблица + ввод задачи двумя коммитами: run-history с тегом
   src/<ID> → дельта-процесс diff по sources/confluence → docs/ +
   строка «Срез источника» в профиле; порядок ввода произвольный,
   состояние master = множество введённых задач). Канон a866f36,
   стенд accept 04cbd4b.
10. ЦЕПОЧКА СОБЫТИЙ (решение владельца 2026-09-10, вечер; термин
    «хронология» вместо «летопись» — везде): future = хронология
    «выгрузка → задача → …» с тегами hist/<ID>, старые теги не
    перестраиваются; новая выгрузка = коммит «Выгрузка: …» на обеих
    ветках (run-history ставит сам, когда «архив + введённые ранее»
    отличается от вершины); левая граница дельты — src/<ID>~1; пустой
    ввод штатен (страницы под флагом чужой невведённой задачи), скрипт
    печатает причину; хвост — по манифесту архива. apply_history:
    введённые ранее из тегов ветки, файл задач = только вводимые, без
    файла = только выгрузка, preflight в dry-run (27 тестов). Проверка
    на одноразовом клоне EAS: два ввода чистые, сборка future = бывшая
    хронология по содержанию (612 файлов, различия только пустые
    строки). Найдено: .bat поставки были LF и не запускались в cmd
    (переведены в CRLF); тег src/PROM отставал от master на 2 коммита
    (переносится владельцем). Гид: раздел «Ветки и теги» переписан.

## Очередь (следующие шаги на выбор владельца)

1. Обкатка координатора v1.0 на STS-04+ КК (владелец открывает сессию
   координатора, агент-владелец канона наблюдает) — рекомендация.
2. Сигнал по треку inkasso (пойдут/нет) — ждём владельца.
3. Политики представления эталона (нумерация H1/ID, пороги деления).
4. Разбор OQ-бэклога стенда (до OQ-051).
5. Параметризация лимита захода (данные: сильная на ~1000 строк = ≤1
   цикл; тянет решение «модель — атрибут этапа»).
6. Д-24 хвосты: сторож связки «ссылка → блок» (ждёт живого случая).
7. С первой сдачей команды — завести журнал сдач (метрика).
8. Живой тест README-маршрутизатора на непогружённом аналитике
   (≤5 мин: выбрать трек + первый файл).
9. EAS: перенести src/PROM на HEAD master; пересобрать future как
   хронологию (branch -f future master → run-history с
   migration-apply-order.md, --tag-prefix hist/, --commit-prefix «Срез
   хронологии»; ~30 мин; push future + refs/tags/hist/*). Роадмап v6:
   переписать Этап 4 целиком + сквозная сверка (суть, карта, словарь,
   этап 5 пп.1/7, завершение) — по фактическому поведению утилиты;
   решить нормативный дом модели веток (рекомендация — роадмап, гид со
   ссылкой). Инструмент дельта-процесса (diff коммита ввода → раскладка
   по page_id, автообновление «Среза источника») — ещё не существует;
   critic diff <ID> — по желанию (локальная точная проверка).
10. Д-26 в реестр при случае: метрика = журнал сдач; инцидент bs4 как
    прецедент «обещание об окружении без проверки».

## Как запускать

- Гейт (из корня стенда): `python ../docs-o2new/_meta/tools/selfcheck.py
  --docs docs --sources confluence --baseline <принятый> --out <отчёт>
  --journal sandbox/journal.txt`
- Тесты analyzer (venv сломан): `PYTHONPATH="$PWD;$PWD/.venv/Lib/site-packages"
  PYTHONIOENCODING=utf-8 <системный python3.12> -m pytest tests/... -q -o addopts=""`
- Accept среза канона: `python <канон>/_meta/tools/accept_canon.py
  --profile <стенд>/README.md` (+ коммит README стенда).

## Ключевые файлы

- Канон: `_meta/migration/` (README-маршрутизатор, инструкции треков,
  гид reference.md, промпты), `_meta/tools/step-protocol.md`
  (протокол, нормативный словарь), `_meta/templates/`, `_meta/skills/`,
  `_meta/services.json` (+ атрибут repo).
- Analyzer: `app/data/Реестр решений по ядру скиллов.md` (Д-1…Д-25),
  `app/scripts/CI/{selfcheck,normalize_tables,link_debts,source_inventory}.py`
  + tests; `app/scripts/apply_history.py` (канон run-history бандла
  confluence-tree-exporter, сборка build_bundle.py); `app/data/Промпт генерации плана миграции сервиса к
  эталону.md` — замороженная линия v1 (командам на v1.x), не живая.
- Стенд: `prompts/` (промпты этапов, STS-03 — образец журнала серии),
  `sandbox/selfcheck-*.txt` (отчёты), `docs/traceability-matrix.md`.

## Незакрытые технические мелочи

- Untracked-мусор в корне analyzer (zip/rar/docx, api-specs*, test.md
  и др.) — в коммиты не брать (гейт: точечный git add).
- Push из агентской сессии заблокирован политикой разрешений — push
  делает владелец (или добавить Bash-permission на git push).
