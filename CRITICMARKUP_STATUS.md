# CriticMarkup — статус и точка возврата

Задача: перенос статуса требований ДБО из Confluence (цвет) в **CriticMarkup**.
ТЗ: `app/data/ТЗ агенту статус требований criticmarkup.md`. Модули 1-3 нужны при любом
исходе развилки п.1.5.

> Это выгрузка проектной памяти агента (точка возврата). Обновлено 2026-07-31.

## ТОЧКА ВОЗВРАТА (статус на 2026-07-31)

**Ветка** `color-jiratask-to-CriticMarkup`. Модуль 1 закрыт целиком (срез 2 / признак 2
«примыкание» — см. раздел ниже). Руководство `critic_manual.md` → `03bb9d6`.
**122 теста зелёных.**

Прогон тестов (venv сломан — системный Python + venv в PYTHONPATH):
```
PYTHONPATH="$PWD;$PWD/.venv/Lib/site-packages" PYTHONIOENCODING=utf-8 \
  /c/Python312/python.exe -m pytest tests/test_critic_*.py tests/test_color_map.py \
  tests/test_migrate_tasks.py -q -o addopts=""
```

### ГОТОВО (Модули 1-3 целиком, всё закоммичено)

- **Модуль 2+3** — `app/scripts/CI/critic.py`: `apply/reject/apply-all/reject-all`
  (правят `.md` in-place, идемпотентно, CRLF байт-в-байт), `lint` (правила E1-E8, W1),
  `list [--manifest]`. Руководство: `app/scripts/CI/critic_manual.md`.
- **Модуль 1** — `app/color_map.py`, `app/utils/style_utils.py` (normalize_color,
  is_black_color+апгрейд, is_ignored_color, ΔE near-black), `app/content_extractor.py`
  (critic_mode / create_critic_extractor, `_is_edit_color`).
- **Боевой вход:** `migrate_confluence_tree.py --tasks --http [--with-images]` — третий режим
  рядом с approved / `--all` (xor с `--all`). Пишет `docs/*.md` с маркерами + manifest +
  отчёт цвета. `page_cache` НЕ менялся (карта строится из `raw_html`). `migrate_colors.py` —
  библиотека + офлайн-харнесс `--html-dir`.
- **Офлайн-бандл** `C:\Users\alexe\PycharmProjects\confluence-tree-exporter` обновлён нашими
  доработками (critic + `--tasks` + `run-critic.bat`). Новых зависимостей нет.
- Последний реальный прогон сервиса (`C:\Users\alexe\PycharmProjects\Onix-transport\received\integrations`):
  15 стр., **12 позиций на ручной разбор** (8 UNKNOWN — постраничные пропуски строк истории;
  1 коллизия; 3 уплощения high). Прогресс 30 → 15 → 12 по мере фиксов.

### ОСТАЁТСЯ (по приоритету)

1. Опциональный **ТЗ 4.5.5** — уточнение medium-случаев по истории версий Confluence
   (точечный запрос предыдущей версии страницы по списку из отчёта). Отдельным ключом
   запуска; основной алгоритм от него не зависит. Пока medium = 0 — практического входа нет.
2. **Модуль 4** — представления (после выбора генератора сайта). **Модуль 5** —
   автоматизация подтверждения (после развилки п.1.5). Не начаты.
3. Мелочи: цвет уровня ячейки/предка в сырых HTML-таблицах не оборачивается (нужен
   run-level разбор); `.gitlab-ci.yml` дать сниппетом (docs-репо отдельный); storage-источник
   истории для восстановления Jira-ключей (снизит UNKNOWN).

### Действия аналитика по последнему отчёту (не код)

Добавить недостающие строки истории на страницах с постраничными UNKNOWN
(`#339966`/`#33cccc`/`#ff9900`/`#ff0000`/`#ff00ff`/`#00ff00`/`#ff6600`/`#00ccff`, по 1-4 шт);
сверить коллизию `#ff00ff`; проверить 3 уплощения high.

---

## Ключевые решения (согласованы 2026-07-27)

- **Канонический вход миграции — HTTP rendered view** (`page_cache.fetch_page_data_via_http`,
  `viewpage.action`), НЕ Confluence API (`body.storage`): на внутреннем контуре API закрыт.
  Резолверы Jira — под rendered-формы, а не под форму-макрос из ТЗ 4.2.г №1.
- **jira-макрос в rendered view сломан**: «Невозможно найти сервер Jira для этого макроса»,
  ключа нет (макросы рассчитаны на внешний Jira, при переносе не менялись). В колонке
  «Задача в JIRA» ключ есть только как гиперссылка `browse/GBO-…` или plain-text.
- **UNKNOWN-\* ожидаем и нормой**: страницы без извлекаемого ключа дают сплошь `UNKNOWN-*` →
  в отчёт. Режим «только отчёт» первого прогона, не ошибка.
- **Обходной путь для восстановления ключей** (проверить на контуре): в режиме РЕДАКТИРОВАНИЯ
  страницы ключ Jira виден. Storage/editor-формат достаётся тем же браузероподобным
  `_confluence_http_get` через `/plugins/viewstorage/viewpagestorage.action?pageId=` или
  edit-эндпоинт. Источник HTML истории проектировался подключаемым (view ↔ storage).
- Карта `цвет→задача` строится только по НЕ-чёрным строкам истории (чёрная = задача на ПРОМ).

## ΔE near-black (ТЗ 4.3.1/4.3.2)

Цвет вне палитры редактора, перцептивно неотличимый от чёрного, → чёрный. `style_utils`:
`_srgb_hex_to_lab` (CIE Lab, D65, линеаризация гаммы), `delta_e_to_black` (CIE76 до
БЛИЖАЙШЕГО элемента `black_colors`), `is_near_black` (порог `NEAR_BLACK_DELTA_E`, default 3,
калибруется по ΔE-столбцу отчёта; default тугой — ловит только очень близкое типа `#0a0a0a`;
`#444444`≈7.6, `#202122` требуют повышения порога). Порядок ОБЯЗАТЕЛЕН (4.3.2): точный-чёрный
→ UI → задача-в-истории → ΔE → UNKNOWN; ΔE строго ПОСЛЕ истории (единый предикат
`content_extractor._is_edit_color`: цвет из color_map = правка даже near-black). near-black НЕ
добавляется в `black_colors` (набор ручной); legacy `is_black_color`/approved НЕ трогали.

## Признак 2 «примыкание» (ТЗ 4.5.2, срез 2 — 2026-07-31)

Зачёркнутый цветной фрагмент вплотную к вставке ДРУГОЙ задачи = черновик, на ПРОМ не был →
отбрасывается, в отчёт с `confidence: medium`. `content_extractor`: `_adjacent_foreign_insertion`
(обход текстовых узлов в ОБЕ стороны — при перекраске вложенность разрушается, направление
заранее неизвестно), `_normalize_gap_text` (nbsp/zero-width/схлопывание), `_nearest_block_ancestor`
+ `_block_tag_between` (условие 3), `_text_node_style`, `_is_fully_struck`. Проверяется ПОСЛЕ
признака 1 и только для ЦЕЛИКОМ зачёркнутого элемента: `_is_strikethrough` истинен и для
элемента, лишь СОДЕРЖАЩЕГО `<s>`, — без ограничителя терялся бы соседний незачёркнутый текст.

Условия разрыва примыкания: непустой текстовый узел (чёрный/near-black/свой цвет/зачёркнутый);
разные блочные предки либо блочный тег между (ловит пустой заголовок внутри одного `<div>`).
Отчётный тракт менять не пришлось — `migrate_colors`/`migrate_confluence_tree` уже пробрасывали
`confidence`, а секция medium в `render_report_md` существовала.

Тесты (ТЗ п.9 №6-9) — `tests/test_critic_extractor.py::TestAdjacencyHeuristic`, 8 шт., 122 зелёных
всего. Каждое условие проверено МУТАЦИЕЙ (отключил → нужный тест упал): усл.1 — тест 8, усл.2 —
тест 7b, усл.3 — тесты 9/9b. Важное: тест 7 (текст БЕЗ атрибута цвета) условие 2 НЕ покрывает —
отсекается раньше по `not norm`; поэтому добавлен 7b с явным `rgb(0,0,0)`.

На корпусе `Onix-transport/received` (2 стр.) medium = 0, вывод `docs/*.md` и отчёт
**байт-в-байт идентичны** базлайну чистого HEAD — регрессий нет.

## Семантика удаления и вложенность (ТЗ 4.5, срез 1 — коммит 328e305)

`{--ID: X--}` = семантика «текст на ПРОМ» (не наблюдаемый признак; прошлый цвет из storage не
восстановим). Устранён over-drop: `_collect_flattened_text` отбрасывает только
зачёркнуто-ЦВЕТНОЕ (черновик), зачёркнуто-ЧЁРНОЕ сохраняет. Одиночное зачёркнутое цветное →
`{--C2--}` (признак 3). Пост-проход 4.5.4 `postpass_drop_contained_deletions` (critic.py):
`{--C2: X--}`, где X входит в состав чужой вставки, отбрасывается; та же логика в линтере —
правило E8 (ошибка, вхождение подстроки), порог `_MIN_CONTAINED_LEN=4`. Отчёт делит уплощения
по confidence high / medium / post-pass.

## Прочие заметки

- Существующая цветовая система (`content_extractor.py`) — бинарный фильтр approved/unapproved;
  для CriticMarkup добавлен ТРЕТИЙ, цветосохраняющий режим. Порог markdown-vs-HTML-таблица —
  `_table_needs_html` (не менять, ТЗ п.11.5).
- Оракул: `reject-all(critic) == approved` держится для прозы; для HTML-таблиц структурные
  правки (целые строки) намеренно расходятся (approved оставляет пустую структуру,
  critic-нотация удаляет строку — critic корректнее).
- Пользователь вручную добавил в `black_colors`: `rgb(8,8,8)`, `rgb(32,33,34)`, `rgb(68,68,68)`,
  `rgb(153,51,102)` (#993366 — визуально чёрный на его дисплее, хотя объективно тёмно-пурпурный;
  ΔE-далёк, потому только вручную).
- Прогон тестов и предсуществующие фейлы описаны в памяти агента (`running-tests`).
